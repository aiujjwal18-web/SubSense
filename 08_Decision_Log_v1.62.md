# 08 Decision Log v1.62

## Document Control

| Field | Value |
| --- | --- |
| Document ID | DLOG-001 |
| Product | SubSense |
| Version | v1.62 |
| Status | Frozen implementation baseline |
| Source of Truth | Historical decision record |
| Depends On | 00_Project_Governance_v1.23 |

## Purpose

This document records the major product, UX, architecture, technical, security, and governance decisions that define SubSense.

It records why decisions exist. It does not redefine the detailed specifications owned by other documents.

## Decision Entry Standard

Each decision should include:

- Decision ID.
- Date or phase.
- Milestone.
- Category.
- Decision.
- Alternatives considered.
- Rationale.
- Impact.
- Affected documents.
- Status.

## Decision Categories

- Governance.
- Product.
- UX.
- Architecture.
- Technical.
- Security.
- Testing.
- Deployment.
- Database.
- Frontend.
- Backend.
- Documentation.

("Development" — the original 8th category above — was never actually used as a category label in the Active Decision Register below; entries instead used the five more specific labels Testing, Deployment, Database, Frontend, and Backend from early on. Corrected this list to match real usage, per the count-reconciliation note under Decision Statistics.)

## Active Decision Register

| ID | Category | Decision | Rationale | Affected Documents | Status |
| --- | --- | --- | --- | --- | --- |
| DEC-001 | Product | SubSense is an AI-assisted subscription decision platform. | Focuses the product on decision support rather than generic tracking. | 01, 02, 03 | Active |
| DEC-002 | Governance | Users retain final control over financial actions. | Protects trust and avoids over-automation. | 00, 01, 02 | Active |
| DEC-003 | Product | MVP is email-first. | Email is practical for reminders and capstone scope. | 01, 10, 12 | Active |
| DEC-004 | Product | SubSense does not cancel third-party subscriptions. | The app does not control external providers. | 00, 01, 02 | Active |
| DEC-005 | Product | Decision Workspace is the primary authenticated home. | Users need to know what needs attention today. | 02, 03, 04 | Active |
| DEC-006 | UX | Use View -> Edit for editable screens. | Prevents accidental changes and supports trust. | 02, 04, 05, 06 | Active |
| DEC-007 | UX | Archive is preferred over delete. | Preserves history and supports recoverability. | 00, 02, 10 | Active |
| DEC-008 | UX | Search is contextual, not a standalone module. | Avoids unnecessary navigation complexity. | 03, 04 | Active |
| DEC-009 | Product | Profile replaces top-level Settings. | Keeps navigation focused on primary workflows. | 03, 04 | Active |
| DEC-010 | Product | Insights contains both AI insights and financial reports. | Combines analysis without adding another top-level module. | 01, 03, 04 | Active |
| DEC-011 | Product | Google Sign-In is primary authentication. | Reduces onboarding friction and is supported by Supabase Auth. | 01, 09, 13 | Active |
| DEC-012 | Technical | Supabase is the system of record. | Provides Auth, PostgreSQL, RLS, and platform fit for MVP. | 07, 10, 12 | Active |
| DEC-013 | Technical | OpenAI powers AI insights. | Enables recommendation and explanation layer. | 01, 11, 12 | Active |
| DEC-014 | Technical | Resend handles product emails. | Separates product email from auth email. | 01, 11, 12 | Active |
| DEC-015 | Technical | Razorpay is Test Mode only for MVP. | Demonstrates premium flow without live payment risk. | 01, 11, 15 | Active |
| DEC-016 | Architecture | Frontend never talks directly to external providers. | Keeps secrets and provider orchestration server-side. | 11, 12, 13 | Active |
| DEC-017 | Architecture | Use hub-and-spoke integration architecture. | Keeps integrations replaceable and mediated by backend. | 11, 12 | Active |
| DEC-018 | Database | Use UUID primary keys plus business identifiers. | Supports joins and human-readable traceability. | 09, 10 | Active |
| DEC-019 | Database | Use PostgreSQL ENUMs for stable shared states. | Improves type safety and reduces repeated checks. | 10 | Active |
| DEC-020 | Security | Enable RLS and least-privilege access. | Protects data even if application logic fails. | 10, 12 | Active |
| DEC-021 | Backend | Services own transaction boundaries. | Prevents inconsistent state and long-running transactions. | 12 | Active |
| DEC-022 | Backend | Background jobs use the same service layer as APIs. | Ensures business rules are applied consistently. | 12 | Active |
| DEC-023 | Frontend | Component-driven frontend architecture. | Aligns with Lovable, Design System, and Component Library. | 13 | Active |
| DEC-024 | Frontend | Separate client state from server state. | Prevents stale duplicated business data. | 13 | Active |
| DEC-025 | Testing | Use a business-first testing pyramid. | Tests the product flows, not just technical units. | 14 | Active |
| DEC-026 | Testing | Use RTM for requirement-to-test traceability. | Prevents frozen decisions from being missed. | 14 | Active |
| DEC-027 | Deployment | Use progressive environments. | Production is never updated directly. | 15 | Active |
| DEC-028 | Deployment | Store secrets only in managed secret stores. | Prevents secrets from leaking into code or frontend bundles. | 15 | Active |
| DEC-029 | Governance | Publication-quality docs are generated after IR-009. | Avoids documenting unstable implementation decisions. | 00, 09 | Active |
| DEC-030 | Governance | API document remains the user's existing `11_API_Integration_Architecture` document as the source of truth for API contracts — this pins the document's identity and origin (existing user document, not Claude-generated), not a frozen version number. It now versions through the same cascade as every other generated document (currently v1.1, amended per the housekeeping note in Version History below). | User already has the completed API document. | 07, 09, 11, 12, 13, 14, 15, 16 | Active |
| DEC-031 | Architecture | SubSense MVP uses Lean Access Architecture: direct Supabase client + RLS for CRUD, Edge Functions only for secret-bearing or cross-cutting logic. | Matches solo/part-time build capacity without changing enforced business rules; RLS becomes the Repository/Service enforcement layer for simple entities. | 07, 11, 12, 13 | Active |
| DEC-032 | Product | Subscriptions record a payment rail (`payment_method`: UPI AutoPay, card e-mandate, app-store billing, or manual) plus an optional reference note. | India-specific cancellation paths differ fundamentally by rail; a UPI mandate can only be cancelled from the originating UPI app, not by SubSense. Surfacing this is cheap and closes the single biggest gap versus India-specific competitors. | 01, 04, 10, 11 | Active |
| DEC-033 | Testing | A dedicated stress-test checkpoint runs at week 3 of implementation, not only bundled into final QA. | Catching RLS, auth-boundary, accessibility, and load issues mid-build leaves time to fix them before the demo, rather than discovering them at the deadline. | 14, 16 | Active |
| DEC-034 | Documentation | Corrected internal title/version drift across 01, 04, 08, 10, 14, 16 (filenames had advanced past internal Document Control fields) and updated stale cross-references in 00, 09, 11, 12, 13, 16 to match. | Filenames and internal versions had diverged, and the governance index and several dependency lists still pointed at superseded versions, risking implementation from the wrong "frozen" source. | 00, 01, 04, 08, 09, 10, 11, 12, 13, 14, 16 | Active |
| DEC-035 | Architecture | Propagated Lean Access Architecture (DEC-031) into 07, 12, and 13, replacing the legacy "API -> Services -> Repositories -> Supabase" backend model and the "frontend never talks to Supabase directly" frontend rule with the two-path model already defined in 11. | DEC-031 named 07, 11, 12, and 13 as affected documents, but only 11 was ever amended, leaving two contradictory backend architectures active in the package at once. | 07, 12, 13 | Active |
| DEC-036 | Database | Added field-level column lists, constraints, indexes, and RLS policy shape for `shared_subscriptions`, `shared_members`, `payment_requests`, `reminders`, `ai_recommendations`, `notifications`, and `payment_transactions`. | These seven tables previously had only a Purpose line in 10, which is not sufficient to generate migrations without an engineer inventing schema during development. | 10 | Active |
| DEC-037 | Security | Refined the `payment_requests` permission model: the parent-owner may update status to any valid value; a linked member may self-report a request as paid, which sets a pending-confirmation state that the owner must confirm before the request is marked fully paid. | 11 allowed "owner or linked member" updates while 10's Table Security Matrix implied owner-only for all Shared-category tables, an unresolved conflict that would break either the "member marks paid" or the "owner confirms paid" flow depending on which document an engineer followed. | 10, 11 | Active |
| DEC-038 | Database | Added `is_premium`, `premium_expires_at`, and `premium_source` to `user_profiles` as the single premium entitlement source of truth, written only by `razorpay-verify-payment`. | 03 assigned premium status to Billing/Profile as its source of truth, but no field existed anywhere in the schema to hold current entitlement state. | 03, 10, 11 | Active |
| DEC-039 | Architecture | Reminder due-date evaluation runs in the user's `user_profiles.timezone`, not server UTC; archived subscriptions are excluded from reminder generation and delivery; Post-Renewal Check-In reminders are generated by a scheduled function evaluating recently-renewed subscriptions, closing the one reminder type in 01 that previously had no generation mechanism. | Reminder generation covered only 3 of the 6 reminder types promised in 01 (7-day, 2-day, renewal-day), and due-today cron logic had no timezone rule, risking missed or duplicate sends for users outside the server's timezone or with archived subscriptions. | 01, 10, 11 | Active |
| DEC-040 | Testing | Added measurable MVP success metrics to 01 (activation rate, time-to-first-subscription, reminder delivery success rate, AI generation success rate, shared-payment completion rate, core flow error rate) and numeric release-gate thresholds to 14's Week 3 checkpoint (load path volume, latency, and error-rate targets). | Success criteria and quality gates were previously binary/task-based with no pass/fail numbers, making "done" a matter of opinion at demo time. | 01, 14 | Active |
| DEC-041 | Product | Added a Retention Strategy to 01: an active-user definition (`last_login_at` within 30 days), a Day 30 retention rate metric (target 40 percent), a `monthly_digest` reminder sent to every user with a non-archived subscription regardless of renewal timing, and a `lapsed_reengagement` reminder for users inactive over 45 days. Added both as new `reminder_type` enum values and their generation/duplicate-prevention mechanisms to 10. | None of the seven DEC-040 metrics measure whether a user stays, and reminders were previously renewal-triggered only, so a user with widely-spaced renewal dates could go months with no touchpoint and no reason to return. | 01, 10, 16 | Active |
| DEC-042 | UX | Finalized SubSense's visual identity: the "Monochrome precision" color system (near-black neutral surfaces, a single restrained indigo accent reserved for identity/CTAs, and a status color scale kept independent of the accent), a two-family type system (Space Grotesk for display/numbers, IBM Plex Sans for body), and a logo (abstracted "S" monogram plus wordmark). Propagated the resulting version bump through every document that cites 05's or its dependents' version numbers. | 05_Design_System previously defined visual direction and component standards with no concrete hex values, typography, or logo, which left Phase 3 (App Shell and Navigation) without a source of truth to build the Global Header against. | 00, 01, 02, 03, 04, 05, 06, 07, 13, 14, 15, 16 | Active |
| DEC-043 | UX | Closed the remaining 05_Design_System gaps named but never defined: a spacing/grid scale (4px base — 4, 8, 12, 16, 24, 32, 48, 64 — with 20px card padding and 24px gap between cards), a uniform card visual standard (12px corner radius, 1px hairline border on Surface 1, no drop shadow at rest), and an icon system (Lucide, single line weight, 20px inline / 24px standalone). Explicitly rejected structural card-to-card imperfection (random rotation, inconsistent radius or padding) as the source of visual variety; variety instead comes from real per-service brand icon color and content, plus micro-interactions and AI copy tone (both still open at the time). | 05_Design_System named "Grid and spacing," "Elevation," and "Icons" as in scope but left all three undefined, leaving Phase 3 (App Shell) and Phase 4 (Subscription Card) without a concrete spec to build against. A proposal to add "human touch" via per-card structural randomness was evaluated and rejected because it would work against the Five Second Rule and No Visual Noise principles (EXP-012, EXP-013) on a finance-decision surface where users scan cards quickly. | 00, 02, 05, 06, 07, 13, 16 | Active |
| DEC-044 | UX | Defined SubSense's full micro-interaction system: a shared timing scale (120ms hover/press, 200ms toggles/dropdowns/toasts, 320ms dialogs and page-level transitions, ease-out on entrances / ease-in on exits); rest, hover, press, and keyboard-focus states for all button variants (primary, secondary, destructive, icon), the interactive card, and the toggle; a button loading state (inline spinner, slightly dimmed, non-interactive, no fill-color change); toast and confirmation-dialog enter/exit motion; an explicit rule that purely informational elements (status badges, the Lifecycle Status Badge, the Renewal Urgency Indicator) receive no hover or motion treatment; a `prefers-reduced-motion` fallback removing stagger, translate, and scale from entrance motion; and a capped load-in stagger (first 6-8 list items only). Also corrected the DEC-043 primary-button and card-hover values, which prototyping showed were not actually visible at rest/on interaction: the button's default fill now uses the brighter accent shade with the darker shade reserved for the pressed state, and interactive-card hover now changes border color and background together rather than a single barely-perceptible border shade. | 05_Design_System's Visual Direction named "restrained motion" but defined no actual values, and DEC-043 shipped spacing/card/icon specs with no matching interaction layer, leaving hover, press, loading, toast, and dialog behavior undefined for implementation. Working prototypes of the initial DEC-043 button and card hover values showed a real usability defect, not acceptable restraint: a primary CTA and an interactive card that don't read until the mouse is already on them work against EXP-002 (User Control First) and EXP-012 (Five Second Rule) in `02_Experience_Strategy`. Keyboard-only focus states were also entirely missing, which is a gap against 05's own Accessibility Standards ("Visible focus states") rather than new scope. | 00, 02, 05, 06, 07, 13, 16 | Active |
| DEC-045 | UX | Added AI copy tone guidance to 02_Experience_Strategy's AI Experience Standard: generated insight text must vary sentence opening and structure across cards in the same session rather than reusing one template with the subscription name swapped in; each figure is stated once per card rather than restated in a second phrasing; contractions are permitted; no manufactured urgency in the copy itself even for Critical renewal status, since the Renewal Urgency Indicator's color already carries that signal; and generated text avoids corporate filler and throat-clearing openers, leading with the fact instead. This is written as a style guide for the `ai-generate-insight` prompt design (Phase 7), not a static string table. | The AI Experience Standard already set a tone boundary (guidance not command, avoid fear-based language) but said nothing about variety, leaving a real risk that a literal prompt implementation would produce the same sentence skeleton for every subscription, reading as mechanical once more than one card is on screen. This closes the third and last of the "human touch" sources named alongside DEC-043 (real brand icon/color, motion via DEC-044, and now copy tone), completing that set. | 00, 02, 05, 06, 07, 13, 16 | Active |
| DEC-046 | Database | Added a nullable `logo_url` text column to `subscription_catalog` (per DEC-043's Card visual standard: real per-service brand icon is the sanctioned source of card-to-card visual variety) and expanded the seeded catalog from 10 to 31 rows. Added two new categories, Music and AI Tools, to the original five (Entertainment, Productivity, Education, Utilities, Other); recategorized `youtube-premium` and `spotify` into Music and `chatgpt-plus` into AI Tools; renamed the `disney-plus-hotstar` row to JioHotstar (Disney+ Hotstar and JioCinema merged into JioHotstar, February 2025) and `spotify` to "Spotify Premium". The 21 new rows and every `logo_url` value were sourced against the user's own curated list (cross-referenced against an earlier 30-item draft) and individually verified — Simple Icons CDN render check for global brands, Wikimedia Commons API lookup for India-specific brands with no Simple Icons coverage. One row (ALTBalaji / ALTT) has no verified logo available from either source and is left null pending manual sourcing. | The catalog previously shipped with only the original 10 seeded services and no stored logo, so Add Subscription's catalog cards had no real per-service branding to display, and the catalog itself was missing common India-market services a user would actually search for. | 10 | Active |
| DEC-047 | Architecture | `post_renewal_checkin` reminders are generated exactly 3 days after `subscriptions.next_renewal_date`, evaluated at 9am in the user's `user_profiles.timezone` — the same `local_date_at_9am_tz()` pattern already used for `seven_day`/`two_day`/`renewal_day`. This is a fixed, non-configurable offset; unlike the three pre-renewal reminder types, it is not exposed via `user_preferences.reminder_default_days`. Also locked in the finalized copy for all 8 `notification_templates` rows (each written with a distinct opening/structure per reminder type, rather than one shared sentence skeleton with only the subscription name swapped in). | DEC-039 established that `post_renewal_checkin` is generated by a scheduled function when `next_renewal_date` "has just passed," but left the exact day offset and time-of-day unspecified — an ambiguity that would have forced whoever builds Phase 6 (Reminder Engine) to invent a number. 3 days lets the actual charge settle on the user's statement before asking "still worth it?", while staying recent enough to be a meaningful gut-check. No schema change: the offset is a business-rule constant applied at generation time using the already-existing `local_date_at_9am_tz()` function, not a new column. | 10 | Active |
| DEC-048 | Deployment | Registered a business domain (`subsense.co.in`) and support email address (`support@subsense.co.in`), reversing the earlier working assumption that no custom domain would be purchased for this capstone build. This unlocks two things that were previously blocked: (1) Supabase Auth custom SMTP can now point at a real verified sender, which restores the ability to edit auth email templates in the dashboard at all (Supabase's free-tier default mailer, as of a recent policy change, no longer allows template edits without custom SMTP configured) and lets the brand-styled Confirm Signup / Magic Link / Reset Password templates actually be used; (2) Resend domain verification can proceed, removing the `onboarding@resend.dev` sandbox sender's restriction to sending only to the developer's own inbox, and allowing the brand-styled `notification_templates` emails to be sent from `support@subsense.co.in` to real recipients. | Email trust and legitimacy are directly relevant to a subscription-management product whose core value proposition involves renewal and payment-adjacent communication — a generic sandbox sender undercuts that. The domain purchase removes two workarounds (Supabase's rate-limited uneditable default mailer, Resend's own-inbox-only sandbox) that were adopted specifically because no domain existed; neither workaround is needed once a verified domain and sender are in place. No schema, architecture, or governing-document content changes as a result — confirmed no document among 00-16 references a specific domain, sender address, or "no custom domain" assumption that needs correcting. | 19 | Active |
| DEC-049 | UX | Adopted "Ledger Dark" as SubSense's revised visual direction, superseding DEC-042's Monochrome Precision palette and two-family type system: a slate-navy surface scale (#0F172A background / #192134 card / #1E293B elevated surface), a two-shade blue brand system (#3B82F6 Primary / #1E40AF Secondary — corrected from an initial generated draft where Secondary read brighter than Primary, which would have inverted the button hierarchy), the existing status color scale re-expressed in the new values, a single IBM Plex Sans family replacing the Space Grotesk/IBM Plex Sans pairing for both headings and body (Space Grotesk retained only as the logo wordmark, flagged in 05 for explicit confirmation), and DEC-043's card corner radius reduced from 12px to 8px (padding, border, and no-shadow-at-rest unchanged). Border Strong, Text Muted, and the Primary button's press-state color were not part of the original generated palette and were inferred from the same value family rather than sourced, flagged inline in 05 for confirmation once seen live. Two pre-existing implementation bugs were found and fixed while wiring this change (unrelated to the visual decision itself): `.env.local` was misplaced under `src/lib/` instead of the Vite project root, so environment variables were never actually loaded, and `VITE_SUPABASE_URL` included a duplicate `/rest/v1/` suffix that would have 404'd every Supabase call. | User-driven aesthetic refresh toward a more literal "financial instrument/ledger" read, chosen from a generated set of visual direction options; the reskin was scoped to color, typography, and radius only and does not change card structure, spacing, motion, or copy tone (DEC-043/044/045 unaffected). | 05, 06 | Active |
| DEC-050 | UX | Extended Ledger Dark (DEC-049) to the product's light-mode surfaces — Supabase Auth email templates (file 24) and Resend notification templates (file 25) — closing the open question DEC-049 left unresolved. Updated 05's Light-mode counterparts table to derive from the same slate/blue family as the dark theme instead of the old Monochrome Precision accent (Surface #F1F5F9, Border #E2E8F0, Text Primary #0F172A, Text Secondary #64748B, Accent #1E40AF — the last reusing Secondary's dark-mode hex for adequate contrast on white, the same logic the original light variant used). Applied matching updates directly to files 24 and 25: the new hex values, card radius reduced 12px to 8px to match the in-app card standard, and typography moved from Space Grotesk (headings/CTA/body) to IBM Plex Sans for everything except the "SubSense" wordmark span, which keeps Space Grotesk as the sanctioned logo exception. | Brand consistency across every surface a user or prospective user sees — in-app product, transactional emails, and any future share previews — was judged to matter more than treating email as a separate, frozen-in-place brand moment; a subscription-management product's emails are especially trust-sensitive (payment and renewal adjacent), so a visibly different, dated palette there would work against the same trust goal DEC-048's domain/sender work was aimed at. | 05, 24, 25 | Active |
| DEC-051 | UX | Extended DEC-050's Ledger Dark light-mode consistency to doc 18 (One-Page PRD), closing a gap DEC-050 left open (18 wasn't in that decision's Affected Documents, and it wasn't clear whether that was deliberate). Updated 18's hardcoded light-mode CSS variables to the same values now in 05's Light-mode counterparts table (surface, border, text, and accent tokens), including two hardcoded `rgba()` accent/status-red uses inline in the markup that weren't routed through the `:root` variables. 18's own extrapolated tokens not literally present in 05's table (`--border-strong`, `--text-muted`, `--accent-hover`) were re-derived from the same Tailwind slate/blue family rather than left stale. 18's status colors (neutral/amber/green/red) were updated to match; `--status-todo`/`--status-todo-bg` were deliberately left unchanged, since those are a PRD-local addition never tracked by 05 in the first place (per doc 19's existing note) and DEC-050's brand-consistency logic only extends to colors 05 actually owns. 18's Benton Sans Book typography is unaffected — that was always an explicit, one-off deviation from 05's type system, not something DEC-049's font change touches. Bumped 18 to v1.3. | 18 is a published, shareable summary a prospective reader or stakeholder might see — the same trust argument DEC-050 made for transactional email applies at least as strongly to a document explicitly meant to represent the product externally. The user confirmed this should follow the same consistency logic rather than staying a frozen snapshot. | 05, 18 | Active |
| DEC-052 | Documentation | Introduced doc 26 (Expanded PRD) as a new, separately tracked governing document, distinct from doc 18's one-page summary: same Ledger Dark light-mode visual system and hero/header content as doc 18, but with each section (Why/Who/Score/What/Guardrails/Build Status/Tech Stack) carrying materially more depth sourced directly from docs 00, 01, 02, 07, 08, 09, 10, and 16, plus one new section not in doc 18 at all — a curated Key Decisions table summarizing 20 of doc 08's most product-relevant entries. Created at v1.0, no prior version. | The user asked to begin PRD work referencing the live one-pager's header content only, with everything else sourced from the governing documents — explicitly choosing an expanded document over a same-length refresh of doc 18, and choosing to track it as a new numbered doc rather than an untracked standalone file. | 00, 08, 19, 26 | Active |
| DEC-053 | Frontend | Frontend implementation moved from Lovable to Cursor (with Claude Code assistance), building directly against the Subsense-web repository — a full replacement, not a hybrid; Lovable is dropped from every document that named it as the app-builder tool (01, 04, 05, 07, 11, 15, 16, 18, 19, 26). Google Sign-In remains the primary authentication method, unaffected and still delivered via Supabase Auth (DEC-011 unchanged) — only the standalone "Google API" tech-stack card is dropped, since it never represented a separate integration. Postman is dropped from the planned stack entirely; API contract testing against document 11 is no longer being pursued through it. The registered business domain (`subsense.co.in`, DEC-048) is added as its own stack entry across the same documents, rather than staying implicit inside DEC-048 alone. | Actual build practice had already moved to Cursor (the Ledger Dark reskin and Supabase env-wiring fixes this session were both done there, directly in the real repo, not through Lovable) without a decision ever being recorded — user review of doc 26's Tech Stack section caught the gap between documented and actual tooling. The user confirmed this is a full tool replacement, that Google Sign-In itself is untouched, and that Postman is no longer part of the plan. | 00, 01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14, 15, 16, 18, 19, 26 | Active |
| DEC-054 | UX | Froze Renewal Urgency Indicator (C-012) day-thresholds against `subscriptions.next_renewal_date`: Critical at 2 days or fewer until renewal, Upcoming at 3-7 days, Normal beyond 7 days (Future: Overdue reserved, not yet in scope). Urgency is derived client-side from the renewal date at read time — no new column or stored value. Thresholds deliberately reuse the already-frozen `two_day`/`seven_day` reminder windows (DEC-039/01/10) rather than introducing a second, independently-tunable "how soon is soon" number. | Phase 4 (Subscription Management) implementation needed a concrete threshold to render the Renewal Urgency Indicator and none existed in any frozen document — 05 named the three states (Normal/Upcoming/Critical) but never defined where the lines fell. Reusing the existing reminder windows keeps "how urgent this looks" and "when the user gets reminded" consistent, rather than risking two silently-different numbers meaning "soon" in different parts of the product. | 05, 16 | Active |
| DEC-055 | UX | Closed the logo wordmark font question DEC-049 explicitly left open for confirmation: the "SubSense" wordmark uses IBM Plex Sans (the `font-heading` token already used for every other heading in the app), not Space Grotesk. Reverses DEC-049's and DEC-050's "Space Grotesk retained only as the logo wordmark" exception. Space Grotesk stays an installed but unused dependency (`@fontsource/space-grotesk`) — no import added anywhere. Also formally implemented the logo itself: an abstracted continuous "S"-curve monogram in Primary (#3B82F6) on a rounded-square tile, as an icon-only mark for in-app navigation (Header/Logo-Home) and a full icon+wordmark lockup for the pre-auth entry screen, built as a token-driven (not hardcoded-hex) component so it tracks the palette automatically. | This was the one piece of DEC-049 left as a flagged, unconfirmed assumption rather than a decision — now that the logo is actually being built in code, the ambiguity has to resolve one way. Matching the wordmark to the app's one existing type family removes a font import for a two-word brand mark, and keeps the wordmark visually consistent with every section heading around it rather than introducing the one deliberate exception DEC-049 anticipated but never actually needed elsewhere in the product. | 05 | Active |
| DEC-056 | UX | The three pre-authentication screens (sign-in/sign-up, forgot password, reset password) carry a deliberate, scoped exception to the frozen Card visual standard (DEC-043/049) and to the No Visual Noise/Five Second Rule principles (`02_Experience_Strategy`, EXP-012/013) as applied to card structure: a live particle/sparkle background (tsparticles-rendered `SparklesCore`, colors sourced from the Primary/Background CSS tokens, not hardcoded), a glass-morphism sign-in card (translucent background, backdrop blur, a blurred Primary-tinted corner glow, a diagonal light-sheen overlay, and a 28px corner radius — versus the standard 8px/opaque/no-shadow-at-rest card used everywhere else), kinetic typography (a reusable `KineticText` component supporting a per-character/word stagger mode and a typewriter mode with a blinking cursor, replayable on hover) applied to the "SubSense" wordmark and a new tagline — "Track smarter. Renew wiser.", replacing the literal "Sign in to track your subscriptions." — and a custom pill-shaped interactive hover button (label-slide/dot-grow reveal on hover) as the Sign in/Create account primary CTA, replacing the standard Button component for that one control. Scoped explicitly to those three screens; every other screen in the product is unaffected and continues to use the standards above unchanged. **Note added under DEC-057:** this exception is no longer expected to be permanent — the pre-authentication screens are scheduled to receive the new brand-kit reskin in a later pass, after the main app. Until that pass runs, this section remains fully accurate and unchanged. | The pre-authentication screens are a single, one-time first-impression surface with no cards to compare against each other and no financial data on screen — the reasoning behind card-to-card structural consistency (preventing users from re-learning a scanning pattern) does not apply there the way it does on Decision Workspace or My Subscriptions. A fully product-wide visual direction change was considered and deliberately deferred as a separate, larger, not-yet-decided question (new illustration assets, a possible Information Architecture change, and at least one reference direction in tension with DEC-045's no-manufactured-urgency principle). | 05, 19 | Active |
| DEC-057 | UX | Adopted a new brand kit (user-supplied "SubSense Brand Color & Typography Style Guide") as SubSense's revised visual direction, superseding "Ledger Dark" (DEC-049/050) as the frozen base system. Color: Primary Accent #FFC800 (vibrant amber — primary CTAs, active highlights, key metrics, hero logo), Secondary Accent #FFFFFF (pure white — secondary display titles, high-priority headers, icons), and a two-tier near-black background split (chosen by the user for text prominence): #050505 for the page canvas (deepest layer, recedes) and #0B0C0E for card/elevated surfaces (slightly lighter, tinted rather than pure black, holds text-bearing surfaces forward) — following the same base-darker/surface-lighter elevation logic Ledger Dark already used. Typography: a four-family system replacing the single IBM Plex Sans family — Syne (Heavy/Ultra-Bold) for the primary display/logotype and hero statements, Cabinet Grotesk (Light/Regular) for secondary display and section titles, Plus Jakarta Sans (Bold) for UI headers, card titles, and button labels, and Inter (Regular/Medium) for body copy, descriptions, and dense/numeric UI text. Card standard: the brand kit's own guidance ("ensure glassmorphic cards sit on top of the deep obsidian base with subtle border contrast") makes glass-morphism the new app-wide card treatment, superseding the opaque Surface-1-fill standard from DEC-043/049 — the existing 8px corner radius is unchanged, since the brand kit does not specify a radius value and there is no reason to move a frozen value the source material does not address. Button styling: Primary buttons use a solid #FFC800 fill with dark (background-base) text, per the brand kit's explicit instruction; Secondary button treatment is not specified by the source (which defines only two accent colors) and is inferred as an outline/transparent-fill treatment using the Secondary Accent (white) for border and text, flagged for live confirmation once seen built, consistent with how prior DECs (043/049) have flagged inferred values. Status colors (Neutral/Amber/Green/Red) and the Border/Border Strong hairline tokens are carried forward unchanged from Ledger Dark — the brand kit does not address them, and status meaning is deliberately kept independent of brand color regardless of which brand palette is active. Light-mode/email counterparts (files 24/25) are not addressed by the new brand kit (a dark-canvas-only system) and are deliberately left on their existing Ledger Dark light-mode values pending a follow-up decision, the same open-then-closed pattern DEC-049 originally left for DEC-050 to resolve. Scope: the whole product, including the three DEC-056 pre-authentication screens — but sequenced with the main app reskinning first and the pre-authentication screens later, so the already-tuned DEC-056 build is not reworked twice in the same pass. Four externally-sourced reference UI components accompany this decision as candidate implementations, none of which run as-is on the current stack and all of which require adaptation before use: an interactive canvas dot-grid background (strip the Framer Sites `addPropertyControls`/`ControlType` import, which has no runtime purpose outside the Framer site builder), a rotating-gradient pill button (missing its `rotatingGradient` keyframe definition, not included in the supplied snippet), a mouse-tracking glowing-border card effect (already compatible — uses `motion/react`, no changes needed), and a collapsible hover-expand/mobile-overlay sidebar (uses `framer-motion` and Next.js `next/link`, both incompatible with this project's Motion-only-via-`motion/react` rule and its Vite/react-router-dom routing — needs conversion on both). No new npm packages are required for any of the four; every dependency they need (`motion`, `react-router-dom`, `lucide-react`) is already installed in Subsense-web. Font loading for the four new families is a genuine new setup requirement, tracked in `NEXT_SESSION_AGENDA.md` as the first step for the next Cursor/Claude Code session — Cabinet Grotesk specifically is not on Google Fonts and needs Fontshare or a self-hosted file. | User-directed identity refresh, chosen over incremental token changes, following the same "one identity decision at a time" pattern DEC-049 established. The two-tier background split follows Ledger Dark's own elevation precedent and avoids using a pure, untinted near-black anywhere text sits, consistent with general UI design guidance the user had separately adopted this session. Sequencing the main app before the pre-authentication screens avoids reworking DEC-056's already-verified, working build a second time in the same pass; carrying forward status colors, hairline borders, and light-mode/email values unchanged avoids inventing values the source material never specified. | 05, 19 | Active |
| DEC-058 | UX | Resolved the treatment of three motion/background candidates discussed after DEC-057 but not yet built. (1) Dropped the interactive dot-grid canvas background candidate outright: the main app uses the flat Page background token (#050505) with no animated background layer at all, not even a paused/static rendering of the dot-grid asset. (2) Scoped the mouse-tracking glowing-border card effect (DEC-057's third reference component) to render only on subscription cards within the DEC-054 Critical or Upcoming renewal-urgency window (2 days or fewer, or 3-7 days, until renewal) — Normal-tier cards (more than 7 days out) keep the plain glass card with no glow, so the effect functions as a real urgency signal rather than a blanket decorative treatment on every card. (3) Added a fifth externally-sourced reference component, Border Beam (a CSS `offset-path` perimeter animation traced around an element's border, user-supplied), scoped to run exactly once on the outer app shell/frame and never repeated per-card — the default `#ffaa40`/`#9c40ff` demo colors are replaced with brand tokens, and its motion is kept low-contrast/slow rather than the demo's punchy loop, since it is meant to read as a single persistent brand signature, not a focal effect. Together, items 2 and 3 establish a deliberate two-tier motion hierarchy for the app: exactly one always-on ambient treatment (the shell Border Beam) and exactly one state-conveying treatment (the renewal-urgency card glow) — no other screen or component gets a third, competing kind of ambient or decorative motion under this decision. | The three candidates discussed following DEC-057 risked flourish-stacking if all three shipped as blanket, always-on treatments on the same screen (a moving background, a glow on every card, and a beam on every card, simultaneously) — multiple simultaneous decorative-motion layers work against 05's own "calm, financially trustworthy... not decorative for its own sake" Visual Direction principle and risk the product reading as a component-library showcase rather than a trusted finance tool, the opposite of SubSense's positioning. Tying the card glow to the already-frozen DEC-054 urgency thresholds reuses an existing, meaningful state boundary instead of inventing a new, independently-tunable "glow zone" number, and turns a purely decorative effect into a real information signal a user can act on. Restricting Border Beam to a single outer-shell instance rather than per-card also resolves a genuine, not just aesthetic, concern: a subscriptions list can run 20-30 rows, and that many simultaneous offset-path perimeter animations rendering continuously (including off-screen, below the fold) is real, avoidable rendering cost for a flourish. | 05, 19 | Active |

| DEC-059 | Product | Reverses part of DEC-043 and DEC-046: subscription cards no longer render a real per-service brand logo. Card iconography is now a fixed category-to-icon mapping using the app's existing Lucide icon system (DEC-043), rendered as an inline component (no image fetch, no URL): Entertainment -> Tv, Music -> Headphones, Productivity -> Briefcase, Education -> BookOpen, Utilities -> Wrench, AI Tools -> Bot, Other -> Layers. The subscription's real name (e.g. "Netflix", "Spotify Premium") still displays as plain text, but only in the app's own type system (Plus Jakarta Sans/Inter per DEC-057) — never a brand's own logotype, wordmark styling, or proprietary font. `subscription_catalog.logo_url` (added under DEC-046) is no longer read by the frontend anywhere; its 30 populated real-logo URLs (Simple Icons CDN / Wikimedia Commons references) are nulled out via a new SQL patch, scrubbing the stored brand-logo references, not just their on-screen rendering. The column itself is retained in the schema, unused, rather than dropped — reversible at low cost if ever needed again, and a column drop is a separate, higher-effort schema change not required to close the actual concern. | Even in a non-commercial capstone build, displaying another company's actual logo artwork and reproducing brand-specific typography carries real trademark/IP exposure that a generic category icon plus the plain service name does not — the user raised this directly and asked for it to be fixed before any other work continues. Using Lucide (already the frozen icon system, already an installed dependency) avoids introducing a second icon mechanism or any new asset-hosting requirement; a category-level mapping is also simpler to reason about and maintain than 31 individually-sourced per-service icons ever were. Nulling the stored URLs (rather than just not rendering them) removes the actual brand references from the data itself, which is a more complete fix than a rendering-layer change alone. | 05, 06, 08, 10, 19 | Active |

| DEC-060 | UX | Replaces the abstracted "S"-curve SVG monogram (DEC-055) with a real, pre-rendered image asset (`src/assets/subsense-logo-icon.png`, cropped from a user-supplied brand concept image) as the icon everywhere it appears — in-app navigation and the pre-authentication entry screen alike — reversing DEC-055's distinction between an icon-only in-app mark and a full lockup reserved for marketing/pre-authentication contexts; the identical real-asset icon now renders in both places, the only difference being whether it appears alone or paired with the wordmark. The wordmark and tagline both move from a single flat color to a two-tone split: "SubSense" renders as "Sub" in Primary (#FFC800) immediately followed by "Sense" in Foreground/white, and the tagline "Track smarter. Renew wiser." (copy unchanged, per DEC-056) renders its first clause in Foreground/white and its second in Primary — matching the color-split pattern in the user-supplied concept image while keeping the already-decided tagline copy and wordmark font (Syne, per DEC-057) rather than adopting the image's own different tagline text or any font change. The `KineticText` component (introduced under DEC-056) gains a new additive prop, `hideCursorOnComplete` (default off, every existing call site unaffected): when a two-tone wordmark or tagline is built as two sequenced `KineticText` instances instead of one, this lets the first segment's cursor disappear at the exact instant the second segment starts typing, instead of the component's existing blink-twice-then-fade behavior, which would otherwise overlap the next segment's own active cursor and show two at once for close to 2 seconds. | Keeping the abstracted SVG monogram and attempting to recreate the concept image's glossy, multi-layer 3D bezel treatment in code was considered and rejected — a hand-built SVG/CSS recreation of that effect carries real fidelity risk and ongoing maintenance cost against a small, pre-rendered image asset that matches exactly. The wordmark and tagline are kept as live, accessible, re-themeable text rather than also rasterized into the image, since only the icon itself required pixel-level fidelity the SVG could not practically match. | User-directed brand refresh: the user supplied a reference image with explicit instructions — same icon treatment in-app and at login, not marketing-only; existing tagline copy and existing wordmark font both kept unchanged; only the image's color-split pattern adopted. | 05 | Active |
| DEC-061 | UX | Retires the single, full-width "Header" bar named under Global Navigation's reusable pattern list (DEC-006 et al.) as a distinct opaque top bar. Replaced by three independent pieces: a background-less "TopBar" strip (mobile hamburger control plus the Logo/Home link — the reusable "Logo/Home" pattern now lives here specifically) spanning the full width above the Sidebar+Main row; a "TopBarActions" cluster (Search, Add Subscription, Notifications, Profile menu — the same controls Header previously held) that floats independently, fixed to the top-right corner of the viewport rather than living inside a shared bar; and the Sidebar+Main content row beneath both, with Sidebar and Main remaining structurally and visually independent of each other (no shared background or frame). | Making Header itself transparent (dropping only its background/border classes, keeping its single-bar structure) was considered and rejected — Header's DOM footprint still spans the full width of the Main column only, leaving the Sidebar-width portion of the top edge unaffected and not resolving the separate layout goal of Sidebar starting below the relocated logo rather than at the very top. | DEC-058's Border Beam (the product's single ambient motion signature, traced around the outer app shell's perimeter) was structurally invisible across the top edge wherever it passed behind Header's opaque, full-width background — painting underneath it rather than being visible through it. Splitting Header into a slim logo/hamburger strip plus an independently-floating actions cluster removes every full-width opaque element from the shell's top edge without losing any of Header's prior functionality. Border Beam's own visibility is not fully resolved by this restructure alone — its animation parameters (`size`/`duration`) were tuned for deliberate subtlety under DEC-058 and may still read as barely visible even with the obstruction removed; a separate parameter retune, plus a decision to extend the existing glow-card effect (DEC-058) to the two highest-importance Decision Workspace cards rather than relocating Border Beam itself, is tracked in `NEXT_SESSION_AGENDA.md`, not part of this decision. | 05 | Active |
| DEC-062 | UX | Extends the existing renewal-urgency glow-card effect (`GlowingEffect`, introduced under DEC-058) to a second, distinct meaning: page-section importance, not subscription-level renewal urgency. Applied unconditionally — no urgency gating — to `DecisionWorkspacePage`'s "Today's Financial Context" and "Recommended Reviews" sections, reusing the exact same component and call pattern already used on Critical/Upcoming/Overdue `SubscriptionCard`s (`<GlowingEffect glow disabled={false} spread={30} proximity={48} borderWidth={2} />` inside a `relative` parent) rather than a second, separately-tuned effect. "AI Insights," "Upcoming Renewals," "Shared Payment Activity," and "Potential Savings" are deliberately left untouched. Alongside this, Border Beam's animation parameters (introduced under DEC-058) are retuned — `size` 200px to 320px, `duration` 30s to 10s, `borderWidth` 1px to 2px, colors unchanged — on its single existing shell-level mount point (`AppLayout.tsx`); a props-only tuning change, not itself a new scope, since DEC-058 already established Border Beam as the shell's persistent ambient signature, and this keeps it within that decision's "quiet, not focal" intent, now actually visible after DEC-061 removed the structural obstruction that hid it. | Relocating Border Beam off the shell and onto the 2 dashboard sections instead of retuning it in place, as originally proposed, was considered and rejected: Border Beam is currently the app's only ambient motion signature across all 5 authenticated destinations (Decision Workspace, My Subscriptions, Shared, Insights, Profile), since it is mounted once in `AppLayout.tsx` outside routed content. Moving it onto 2 cards on one page would remove that signature from the other 4 destinations entirely as a side effect of a visibility fix, trading one problem for a worse one. | `DecisionWorkspacePage`'s sections carried no visual distinction from each other despite "Today's Financial Context" and "Recommended Reviews" being its two highest-priority destinations for a user's attention — reapplying the same glow language already proven legible on `SubscriptionCard` achieves that distinction without inventing a new visual effect. Recording this as its own decision, rather than treating it as an unremarkable reuse, keeps `GlowingEffect`'s two meanings (renewal urgency vs. page-section importance) explicit so a future third use doesn't quietly blur the distinction further without a similar check. | 05 | Active |
| DEC-063 | UX | Reverses DEC-057's "GradientButton... not yet a complete, working component... not adopted into the frozen standard" note: the rotating conic-gradient ring treatment is folded into the shared `buttonVariants` system as a proper `variant="gradient"` (`src/components/ui/button.tsx`), rather than remaining the standalone, hand-rolled `GradientButton` component (a `<div role="button">` with no real focus-visible ring or disabled-attribute semantics). Scoped to the 5 real primary "Add/Save Subscription" call-to-action buttons only — `TopBarActions`, `DecisionWorkspacePage`'s and `SubscriptionsListPage`'s empty-state buttons, and `AddSubscriptionPage`'s "Save Subscription" submit (a fourth, `SubscriptionsListPage`'s own list-header button, was subsequently removed entirely as a redundant duplicate of `TopBarActions`' persistent floating button, not because the gradient treatment itself was wrong) — not adopted as a universal button treatment; every Secondary, destructive, and icon button keeps its existing flat/outline treatment untouched. Includes a `prefers-reduced-motion` fallback (Tailwind's `motion-reduce:` variant, freezing rotation to a static 3-color gradient) — the first such handling anywhere in the codebase, scoped to this variant only. The background is delivered via an inline `style` prop rather than a Tailwind arbitrary-value class, after a real rendering bug (the ring initially rendered as a solid, non-rotating amber fill) was traced through the generated CSS, `@property`/`@layer` order, and a fresh rebuild before concluding inline-style delivery (matching the original `GradientButton` reference's own approach) was the highest-confidence fix. | Adopting the gradient treatment universally across every button variant, as DEC-057's original candidate note implied it might become, was not pursued — doc 05's Button rules already establish Primary/Secondary/Destructive/Icon as a deliberate hierarchy, and a moving, animated treatment on every button in the app (including destructive/icon controls) would work against the "calm, financially trustworthy" Visual Direction principle DEC-058 already reasoned about for Border Beam and glow-card. | DEC-057 flagged the rotating-gradient button as a candidate whose supplied reference code was incomplete (missing its `rotatingGradient` keyframe) and explicitly not adopted; the keyframe has since been added (backing this variant, not a standalone component) and the treatment has now been evaluated live and scoped deliberately to the small set of buttons that most benefit from extra visual weight — the app's primary conversion action (adding/saving a subscription) — rather than either staying an unused, half-built candidate or being adopted everywhere by default. | 05 | Active |
| DEC-064 | Product | Adds card-level Paid/Paused/Resume quick-actions to `SubscriptionCard` (`src/components/subscriptions/SubscriptionCard.tsx`), a new write path alongside the existing View -> Edit pattern rather than a replacement of it. Paused excludes a subscription from renewal-urgency calculations, "Upcoming Renewals," and "Recommended Reviews" entirely — `computeRenewalUrgency` takes an optional `lifecycleStatus` parameter that short-circuits a paused row to "normal," and every one of its 5 real call sites (plus a previously-missing filter on "Upcoming Renewals," which had no filtering of any kind before this) was updated so the exclusion is real, not just a badge-level suppression. A paused card shows only a "Resume" button (returning `lifecycle_status` to "active"); an archived card shows neither button; every other card shows both "Paid" and "Paused." Paid opens a new `MarkPaidDialog` (pick-date -> confirm two-step flow, reusing the existing `Dialog` primitive and the native `<input type="date">` pattern already used in `AddSubscriptionPage`, zero new dependencies) and, on confirm, recomputes `next_renewal_date` only: the new anchor date is whichever is later, the current `next_renewal_date` or the entered payment date, plus one full billing interval computed by a new `computeNextRenewalDate` helper (same rule for `custom` intervals as `monthly`/`every_28_days`/`yearly`, no special-casing). `lifecycle_status` — specifically the schema's currently-dormant `renewal_confirmed` value — is deliberately left untouched by Paid. Scoped to `SubscriptionCard` only — `SubscriptionDetailsPage` and `DecisionWorkspacePage`'s row items do not gain these buttons, though both remain real `computeRenewalUrgency` call sites that do respect the paused-exclusion fix above. | Also setting `lifecycle_status` to `renewal_confirmed` when Paid is confirmed — activating a dormant enum value already defined in the schema and, per its badge label, apparently intended for exactly this kind of use — was seriously considered and rejected for this pass: nothing today defines what would transition a subscription back out of `renewal_confirmed` as its next renewal approaches, so activating it here without that reversion logic would risk subscriptions becoming permanently stuck in a "Renewal Confirmed" state that contradicts its own implied, temporary meaning. Treated as a second, not-yet-designed feature (entry and exit conditions for a status lifecycle) rather than a side effect of this one; revisit only as its own explicit decision if wanted. | Phase 6's reminder engine is not built yet, so "Paused stops reminders" is implemented today as exclusion from the three things that currently exist and would otherwise treat a paused subscription as live: urgency calculation, Upcoming Renewals, and Recommended Reviews — revisit once Phase 6 ships real reminders, to confirm paused subscriptions are excluded there too. Confining the payment-date recompute to `next_renewal_date` only, and confining these buttons to the card (not `SubscriptionDetailsPage`/`DecisionWorkspacePage` rows), keeps this pass's actual write surface and scope exactly as small as the locked behavior required, rather than opportunistically expanding either while already touching this code. | 05, 06 | Active |
| DEC-065 | UX | Adopted a new user-supplied brand kit, "Cyber Lime," as SubSense's revised visual direction, superseding the amber/white kit (DEC-057) — a full token-level reskin comparable in size to DEC-057 itself. Color: Canvas unchanged (#050505); Surface 1/Surface 2 reintroduced as two distinct tiers (#121212/#1A1A1E — DEC-057 had collapsed them to one shared value, since its own source kit gave only one card/elevated tier; this kit gives two again); Container Border moves from an alpha-white token to a solid hex (#2A2A2E), with Border Strong kept as its own second tier (an inferred value, ~#3A3A3F, same Zinc family) rather than collapsed to the source kit's single border value — Border Strong has a real, live job today (the Secondary button's hover-state border), not spare capacity, so collapsing it would have silently removed a working state distinction; Primary Accent moves from amber (#FFC800) to Cyber Lime (#A3E635) — the headline change, touching every Primary fill, active-state highlight, and key-metric emphasis; a new Secondary Accent role (#38BDF8, "Cool Steel Blue") is added, reserved for the wordmark gradient plus status-tag/interactive-state uses, deliberately not extended to the button-ring/Border Beam/GlowingEffect gradient pairing (those recolor to lime + Primary Text/near-white instead, keeping blue exclusive to the wordmark so it stays a rare, special treatment rather than a color that shows up on every decorative surface in the app); a new explicit Disabled token (#334155) is added for future use without retrofitting the existing, already-uniform `disabled:opacity-50` utility, since no component currently renders an explicit disabled fill and there was no visible complaint to fix. Status colors (Neutral/Amber/Green/Red) carry forward unchanged — the brand kit defines no new status hex values — but the existing "status meaning is independent of brand accent" rule is now reinforced with an explicit 60-30-10 usage-discipline note: status colors must stay confined to small indicators (badge/border/dot) and never fill a card background or the canvas, confirmed via full-codebase investigation to already be true everywhere today (no violation found to fix). The Secondary button variant is rewritten from a hover-only-emphasis outline treatment to a persistent lime border plus tinted fill, with a genuine 3-step base/hover/active progression (0.08/0.14/0.20 opacity) rather than the two states it had before. The wordmark gains a 45-degree lime-to-blue gradient on "Sense" specifically, applied consistently across all three places the literal text wordmark renders in the app — confirmed via code investigation that these are the only three: `AuthPage.tsx`, `ForgotPasswordPage.tsx`, `ResetPasswordPage.tsx` (the main authenticated app's `TopBar` renders the icon only, no wordmark text, so this is already a narrow, contained surface, not a conflict with 60-30-10 restraint). `AuthPage.tsx`'s kinetic/typewriter wordmark and tagline animation is dropped entirely in favor of plain static text as part of this same pass — pulling forward and resolving part of DEC-056's own open "static vs. animated" question, since it also simplifies the gradient implementation (a static span takes the gradient cleanly; the animated version's blinking cursor would otherwise need its own separate color-conflict fix). `KineticText.tsx` is deleted outright, confirmed to have zero other consumers anywhere in the codebase. Tagline copy changes from "Track smarter. Renew wiser." to "Think wiser. Choose smarter." Typography, light-mode/email counterparts (files 24/25), and the logo icon asset itself are all explicitly out of scope for this pass (see Alternatives/Rationale). | Extending the wordmark's lime-to-blue gradient to the button-ring/Border Beam/GlowingEffect pairing as well (one consistent gradient identity across the whole app) was considered and rejected — this would turn a deliberately rare, special-moment color pairing into a recurring decorative texture on every ambient effect in the app, undermining the exact restraint the wordmark scope decision was built on. Collapsing Border tiers to the source kit's single value was considered and rejected for the reason above (a real, working state distinction would be silently lost). Replacing the logo icon now, using the user-supplied reference image, was considered and rejected — the reference is a glossy, multi-layer 3D concept render, not a flat production-ready asset, and using it as-is would not scale down cleanly to a 16-32px nav icon; the current `subsense-logo-icon.png` (DEC-060) stays in place, flagged as a separate follow-up once a real asset exists. A new hover-surface value (`#242428`) was inferred for the `--muted`/`--sidebar-accent` tier rather than left unchanged, after checking the math: the prior value (`#141517`) sat only a few RGB units from the new Surface 1 (`#121212`), which would have made hover feedback on Surface-1-backed rows and items nearly invisible — a functional regression, not a style nitpick. | User-directed identity refresh, following the same "one identity decision at a time" pattern DEC-049/057 established, layered with a genuine `60-30-10` design-composition discipline applied throughout rather than treated as decorative window dressing: dominant neutral canvas/surface, restrained secondary structure, and a small, protected accent tier that stays meaningful precisely because it isn't overused. Status colors, light-mode/email tokens, and full button hover/press/focus states beyond what's specified here are carried forward unchanged or deferred, avoiding invented values the source material doesn't justify, the same discipline every prior brand-kit decision in this log has followed. | 05, 06 | Active |
| DEC-066 | UX | Retires DEC-056's pre-authentication visual exception, now that DEC-065's Cyber Lime reskin has shipped and the three pre-auth screens (sign-in/sign-up, forgot password, reset password) can land on the new standard tokens once rather than being reworked twice. Two of DEC-056's four pieces were already resolved under DEC-065 (the kinetic wordmark/tagline motion, now static; the tagline copy, now the product-wide "Think wiser. Choose smarter." on all three screens rather than reverting to the screens' original literal copy). What this decision retires: the particle/sparkle background (`SparklesCore`, `src/components/ui/sparkles.tsx`, confirmed used in exactly these three places and nowhere else) is removed outright with no replacement effect, and its three backing `@tsparticles/*` npm packages are removed from `package.json` along with it; the 28px-radius glass-morphism card (translucent, blurred, corner glow, light-sheen overlay) on all three screens collapses to the standard opaque card treatment — full-codebase investigation found the shipped app's actual "standard" card is not one single pattern (`SubscriptionCard.tsx`'s repeated list tile uses a translucent `bg-card/70 backdrop-blur-md` glass treatment; Dashboard and Details' 9 page-level section instances use a fully opaque `bg-card` with no blur), and a full-page pre-auth card is structurally a page-level container like those 9 instances, not a repeated tile, so it converges on the opaque treatment; the custom pill-shaped `InteractiveHoverButton` (label-slide/dot-grow hover reveal), used only on `AuthPage.tsx`'s two submit buttons, is replaced by the standard `Button` component and deleted outright (confirmed zero other consumers) — `AuthPage`'s Sign in/Create account CTA specifically takes `variant="gradient"` (DEC-063's existing treatment, matching the app's highest-prominence CTAs), a deliberate "front door deserves the emphasis" distinction from `ForgotPasswordPage`/`ResetPasswordPage`'s submit buttons, which stay on `variant="default"` and are not touched. Two small follow-up polish fixes shipped in the same pass once the surrounding elements were standardized and made a pre-existing, unrelated issue newly visible: the "Continue with Google" button's hand-added `rounded-full` override (no documented rationale anywhere in the codebase, and this project's Design System locks a single standard radius with no carved-out exception for this button) was removed, falling back to the standard radius; and the hero/card layout gap, which read as a stark, oversized void once the particle background and glass effects that previously gave it visual texture were removed, was tightened by replacing `justify-between` with `justify-center` and removing a redundant edge margin, reusing the row's own existing `gap-16` value rather than inventing a new number. | Reconciling `ForgotPasswordPage`/`ResetPasswordPage`'s CTA variant to match `AuthPage`'s new gradient treatment (full three-screen consistency) was considered and rejected — the "front door vs. utility flow" distinction is a real, deliberate one, not an oversight to fix later; forcing all three to match would have expanded DEC-063's original 5-CTA gradient scope further than intended for two utility screens with no comparable "first thing every user sees" role. Leaving the pre-auth card's padding at `p-8` rather than reconciling it to the majority `p-6` value found during investigation was also considered and confirmed deliberate, not an oversight: padding is not one of this project's locked brand tokens (color/typography/radius/motion are; spacing is not), the "standard" cards do not already share one padding value in practice (`SubscriptionCard.tsx`'s tile uses `p-5`; Dashboard/Details' page-level sections use `p-6`), and a pre-auth card's role — a single, standalone focal element on an otherwise-empty page — is structurally closer to a centered dialog than a dense in-page section, justifying `p-8`'s extra breathing room. | DEC-056 always described its exception as scoped and non-permanent, explicitly noting under DEC-057 that a dedicated reskin pass would retire it later, sequenced after the main app so the already-tuned build wasn't reworked twice — this is that pass. Retiring the particle/glass/button pieces together, in one pass, rather than piecemeal, matches how the exception was granted as one bundled decision in the first place. | 05, 06 | Active |
| DEC-067 | UX | Fixes two Decision Workspace display bugs surfaced during live review, both scoped to `DecisionWorkspacePage.tsx`. (1) `upcomingRenewals` gains a second filter — `computeRenewalUrgency(row.next_renewal_date, row.lifecycle_status) !== "normal"` — dropping any subscription whose renewal sits beyond the existing DEC-054 Upcoming ceiling (itself reusing DEC-039's `seven_day` window); the array previously had no urgency filter at all, just a paused exclusion (DEC-064) plus a date sort and a 5-row cap, both left unchanged. The explicit `lifecycle_status !== "paused"` filter is kept alongside the new urgency filter — even though the urgency short-circuit alone would also exclude paused rows — for the same reason DEC-064 added it explicitly the first time: not relying on an incidental side effect for a stated exclusion rule. Overdue rows stay visible (`"overdue" !== "normal"`). (2) "Recommended Reviews" — previously a `recommendedReviews` computed array (`urgency !== "normal" OR lifecycle_status === "review_due"`, capped at 5) rendering real `SubscriptionListItem` rows through a `relative` wrapper with `GlowingEffect` (per DEC-062) — is replaced by a static placeholder matching the exact structure and copy convention already used by the screen's three genuinely not-yet-built sibling sections (AI Insights, Shared Payment Activity, Potential Savings): no `relative`/`GlowingEffect`, one plain future-framed sentence ("Recommended reviews will appear here once there's something worth your attention."), no internal phase name surfaced. This is a partial, scoped reversal of DEC-062, which had applied `GlowingEffect` to this section unconditionally — the glow no longer applies now that the section renders no dynamic content. Before finalizing, every `lifecycle_status` write path in the codebase was grepped to check whether dropping the deleted array's `review_due` OR-clause would silently hide a reachable, independent signal — none found (`SubscriptionCard.tsx`'s pause/resume toggle only ever writes `active`/`paused`; `SubscriptionDetailsPage.tsx`'s archive only ever writes `archived`; `AddSubscriptionPage.tsx`'s insert never touches the column) — `review_due` is a dormant enum value today, the same situation DEC-064 already flagged for `renewal_confirmed`. No OR-clause was added as a result; flagged here so it is revisited if Phase 7 or a future migration ever starts writing `review_due`. | Both sections drew from the same single fetch (`rows`, all non-archived subscriptions), with `upcomingRenewals` applying no urgency filter and `recommendedReviews` already filtering to `urgency !== "normal"` — with typical data this produced two arrays holding the same rows, rendered through the identical `SubscriptionListItem`/`RenewalUrgencyBadge` components, defeating both the screen's stated purpose (surface what needs attention, not repeat it twice) and Upcoming Renewals' own literal meaning (a subscription paid today reappearing on the list tomorrow serves no one). Recommended Reviews was never actually wired to real AI-generated insight content (Phase 7 / C-003 AI Decision Card, not built) — showing populated, real subscription rows there misrepresented it as a live, distinct feature rather than the placeholder it actually was. The user confirmed the slot should stay reserved, not removed, since Phase 7 is expected to fill it later, and that the honest-placeholder tone should match its three real siblings rather than literally say "Coming soon." The 7-day cutoff reuses two already-frozen numbers (DEC-054's Upcoming ceiling, DEC-039's `seven_day` reminder window) rather than inventing a third "how soon is soon" figure. **Not listed as an affected document this pass:** doc 04's Decision Workspace Blueprint still names a single "AI Insight" line under Primary content, with no "Recommended Reviews" entry at all — the shipped screen has effectively split that one blueprint line into two placeholder sections ("AI Insights" and "Recommended Reviews"). This is a real, pre-existing doc-vs-code gap, not introduced by this decision, but this pass does not resolve it — flagged as an open question for a separate decision on whether/how to update doc 04's blueprint language, tracked in `NEXT_SESSION_AGENDA.md` rather than resolved silently here. **Resolved (same-session follow-up):** the user confirmed "AI Insights" and "Recommended Reviews" are one concept split across two UI slots for now, not two distinct Primary content items — doc 04's Primary content list stays a single "AI Insight" line, with a new Implementation notes bullet added under Decision Workspace Blueprint stating the two-slot split explicitly and citing this decision (doc 04 bumped to v1.18). | 04, 06 | Active |
| DEC-068 | Architecture | Formalizes Phase 6's reminder-scheduling platform and job shape, closing the gap where `10_Database_Architecture` and `11_API_Integration_Architecture` each described Path B reminder scheduling only generically ("a scheduled function (Path B, cron)"), with `monthly_digest`/`lapsed_reengagement` generation not named as a mechanism anywhere in doc 11 at all. Platform: **Supabase Cron** — the hosted dashboard wrapper over the same `pg_cron`/`pg_net` engine every Supabase project already has, chosen over hand-written raw `pg_cron` SQL (no material benefit over the dashboard wrapper for this scale) and over an external third-party HTTP scheduler (would add a new dependency and secret with no corresponding benefit, since DEC-031's Path B rationale is already satisfied by an in-platform scheduler). Job shape: **two separate Edge Functions**, not one. (1) `send-reminder-email` (already named in doc 11 5.2) runs **hourly**, selecting `WHERE status = 'pending' AND scheduled_for <= now()` — a direct instant comparison, simpler than and equivalent to the prior "UTC day boundary in `timezone_snapshot`" phrasing, since `scheduled_for` is already a precomputed absolute UTC instant per DEC-039. (2) A new `generate-scheduled-reminders` (doc 11 5.7) runs **daily**, generating rows for the three types with no other generation path: `post_renewal_checkin`, `monthly_digest`, `lapsed_reengagement` — all day-granularity events that do not need hourly evaluation. Reliability, at MVP scale: no dedicated retry or fallback system is built. Two properties of the existing schema/query design already provide it for free — a reminder is only ever set `status = 'sent'` after Resend confirms delivery, never optimistically beforehand, so a mid-run failure simply leaves it `pending`; and the delivery job's `<= now()` (not `= now()`) comparison means any reminder missed by a skipped or delayed cron tick is automatically picked up, unchanged, by the next successful tick. Active alerting or monitoring on cron-job failures themselves (as opposed to individual reminder-send failures, already handled above) is explicitly out of MVP scope, deferred to production scale and tracked in `NEXT_SESSION_AGENDA.md`, not built now. | Supabase Cron matches DEC-031's Lean Access Architecture rationale (Path B for anything scheduled) without introducing a new external dependency or secret, and gives a free run-log/UI over the same underlying engine raw SQL would use anyway. Splitting generation (daily) from delivery (hourly) matches each job to the actual granularity of what it evaluates — day-level events don't benefit from hourly checks, and forcing all reminder logic through one job would make that one job's failure mode block every reminder type at once. Confirm-before-marking-sent and a `<=`-based catch-up query are both consequences of query design already achievable within the existing `pending`/`sent`/`skipped_archived`/`failed` status enum (DEC-039) — building a separate retry/fallback system on top would be redundant infrastructure for a capstone-scale user base, and Supabase Cron/`pg_cron` itself does not retry skipped runs or alert on failure regardless, so an external monitoring layer would be needed to change that meaningfully — a production-scale investment, not an MVP one. | 08, 10, 11, 12 | Active |
| DEC-069 | Database | Ships the DB-side migration implementing DEC-068's `generate-scheduled-reminders` mechanism (`28_SubSense_Scheduled_Reminders_Patch_v1.0.sql`). Two schema changes: (1) both `reminders_target_present` and `reminders_shared_payment_target` CHECK constraints (`17_SubSense_Migration_v2.sql`) are loosened to also permit `subscription_id` AND `payment_request_id` both null when `reminder_type IN ('monthly_digest','lapsed_reengagement')` — these two types are user-level, with no natural subscription or payment-request row to anchor to, and every other reminder_type's existing requirement is unchanged. `reminders_shared_payment_target` specifically was never documented in `10_Database_Architecture` at all before this pass — only `reminders_target_present` was listed under Constraints — a real pre-existing doc-vs-schema gap closed by this decision, not introduced by it. (2) A new `audit_action` enum value, `reminder_generation_failed`, added via the same two-step (`alter type ... add value`, then a separate statement batch) pattern `20_SubSense_Retention_Patch_v1.0.sql` already established, so `generate_scheduled_reminders()`'s per-row failure logging (doc 11 §5.7's `GEN_001` error code) has a value distinct from `reminder_executed` (which means a reminder was actually sent, not that generation failed). | Anchoring `monthly_digest`/`lapsed_reengagement` rows to an arbitrary subscription instead of loosening the constraints was considered and rejected during Edge Function planning (pre-dates this entry) — it silently fails for a user with zero active subscriptions, exactly the audience `lapsed_reengagement` exists to reach. For the audit value: reusing `reminder_executed` with a metadata flag was considered and rejected — it would overload a value that already has a clear, different meaning (an actual send, not a generation failure), and `audit_logs` gets read by nothing today that would need the JSON-field disambiguation. Skipping audit-log-level failure logging entirely for this pass was also considered and rejected — doc 11 §5.7 already commits to `GEN_001` being logged to `audit_logs` as the stated error-handling mechanism; not implementing it would leave a locked spec unmet. | Closes the gap between doc 11 §5.7's architecture (locked under DEC-068) and an actual runnable migration, and corrects a real, pre-existing doc-vs-schema gap (`reminders_shared_payment_target` undocumented) found while drafting it. | 08, 10 | Active |
| DEC-070 | Database | Ships `29_SubSense_Reminder_Lifecycle_Patch_v1.0.sql`, fixing a real bug surfaced live once DEC-069's Edge Functions started actually delivering: `generate_default_reminders()` (`17_SubSense_Migration_v2.sql`) inserts fresh `seven_day`/`two_day`/`renewal_day` reminders whenever `next_renewal_date` changes, but never cancelled the *old* pending reminders tied to the previous cycle — and no trigger reacted to `lifecycle_status` entering `'paused'` at all, the exact gap DEC-064's own text flagged and explicitly deferred ("revisit once Phase 6 actually ships real reminders"). Confirmed live: after today's auth/domain fixes let the reminder backlog send, a subscription paid forward to late August still delivered "renews today"/"renews in 7 days" emails, and a Paused subscription delivered all three of its default reminders. Worse than cosmetic: `10_Database_Architecture`'s existing partial unique index (`UNIQUE (subscription_id, reminder_type) WHERE status = 'pending'`) means a stale pending row doesn't just deliver wrong content, it silently blocks the next cycle's reminder of the same type from ever being inserted (`ON CONFLICT DO NOTHING` no-ops against it) — a subscription paid/renewed more than once could go reminder-silent entirely, not just receive one extra stale email. Fix: a new `reminder_status` value, `skipped_superseded` (one generic value covering both causes — the reminders row's own `subscription_id`/`updated_at` plus the subscription's current state already lets anyone reconstruct which applied, so a second, narrower value would add schema surface without adding recoverable information); `generate_default_reminders()`'s insertion loop extracted into a callable helper, `generate_default_reminders_for_subscription()`; and a new trigger, `subscriptions_cancel_stale_reminders` (named to fire alphabetically before the existing `subscriptions_generate_default_reminders`, so cancellation always commits before the new insert attempts), that cancels stale pending `seven_day`/`two_day`/`renewal_day`/`post_renewal_checkin` reminders whenever `next_renewal_date` changes or `lifecycle_status` enters `'paused'`, and regenerates the default set via the helper when a subscription resumes from `'paused'` (its `next_renewal_date` is unchanged across a pause, so the existing generate trigger never fires on resume alone — without this, a resumed subscription would go reminder-silent until its next Paid action). Run against the live Supabase project and verified via read-only queries (enum value, trigger firing order, function shapes all confirmed). | Two narrower enum values (e.g. `skipped_renewal_updated` and `skipped_paused`) instead of one generic `skipped_superseded` were considered and rejected — the cause is already reconstructable from the reminder row's own context, so a second value would be schema surface without new information. Backfilling/correcting the reminders already wrongly sent today (the ones that surfaced this bug) was considered and rejected as part of this migration — this patch only changes behavior going forward; a one-off cleanup of already-sent rows, if wanted, is a separate manual action the user can take after confirming which rows are actually stale, not something a schema migration should do automatically. Not regenerating reminders on Resume (leaving the subscription reminder-silent until its next `next_renewal_date` change) was considered and rejected — it would make Paused/Resume something other than a full, symmetric toggle, contradicting DEC-064's own framing of Resume as fully restoring normal behavior. | Closes a gap DEC-064 explicitly flagged and deferred pending Phase 6's real reminder engine (DEC-068/069) — now shipped, so the deferred risk was due to be checked, and today's live delivery confirmed it was real, not hypothetical. | 08, 10 | Active |
| DEC-071 | Database | Ships `30_SubSense_Post_Renewal_Checkin_Paused_Fix_v1.0.sql`, closing a narrow residual gap found while walking DEC-070's fix through every subscription-state transition with the user, not from live evidence: `generate_scheduled_reminders()`'s (`28_SubSense_Scheduled_Reminders_Patch_v1.0.sql`) `post_renewal_checkin` query checked `s.archived_at is null` and `u.account_status = 'active'`, but never the subscription's own `lifecycle_status`. DEC-070's new cancel trigger only reacts to the *transition into* `'paused'` — it does nothing to stop this daily job from generating a brand-new `post_renewal_checkin` afterward, if a paused subscription's frozen `next_renewal_date` happens to land exactly 3 days in the past on some later cron run. Same category of gap DEC-070 itself closed for the three `generate_default_reminders()` types, just on the daily-generation side rather than the state-transition side. Fix: `CREATE OR REPLACE` on `generate_scheduled_reminders()`, adding `and s.lifecycle_status <> 'paused'` to the `post_renewal_checkin` query's WHERE clause — the only change from the version file 28 shipped; `monthly_digest`/`lapsed_reengagement` are untouched (user-level, not subscription-anchored, not affected by a single subscription's paused state). | No real alternative considered — this is a one-clause addition closing an omission, not a design choice between options. Folding it into a broader DEC-070 amendment instead of its own entry was considered and rejected, since DEC-070 was already recorded and cascaded as shipped before this gap was found; a separate entry keeps the traceability chain accurate (this fix was found and shipped later, in its own pass). | A narrow, previously-unnoticed edge case surfaced by deliberately walking every subscription-state transition against the fixed trigger logic, rather than left latent until it happened live the way DEC-070's own bug did. | 08, 10 | Active |
| DEC-072 | UX | Corrects a live drift found by comparing doc 24's source HTML (`24_SubSense_Supabase_Auth_Email_Templates_v1.1.html`) against screenshots of what's actually pasted into Supabase's Authentication -> Emails -> Templates tab: Confirm signup, Magic link, and Reset password were all live as plain, unstyled `<h2>`/`<p>`/`<a>` markup, not doc 24's branded "Ledger Dark" light-mode blocks (white card, `#1E40AF` button, IBM Plex Sans, SubSense wordmark span, footer border). Fix is a straight re-paste of doc 24's existing HTML, verbatim — no new design work, no content or copy change, closing an actual-vs-documented gap rather than deciding anything new for these three. Separately, confirmed Supabase auto-generates a 4th auth template ("Confirm your new email address," the change-email flow) that doc 24 never specified, designed, or documented at all. Decided to leave it as Supabase's plain default copy for now, out of MVP scope for brand styling, rather than design a 4th template — doc 24 updated to record the gap explicitly so it isn't mistaken for an oversight the next time this file is read. | Designing and shipping a 4th branded template now was considered and rejected — change-email is a rare, already-authenticated action, not a new user's first touchpoint (signup) or an elevated-trust-stakes flow (password reset), so brand-styling it isn't worth the effort at MVP scope; revisit if/when doc 24's other 3 templates get a real polish pass. | Found via direct comparison of doc 24's source against the live Supabase Templates tab, not invented after the fact — real drift between documented and shipped state, the same category of gap this project's citation/staleness audits exist to catch. | 24 | Active |
| DEC-073 | UX | Resolves the light-mode/email accent gap DEC-057 opened (superseding "Ledger Dark" blue with the amber kit in-app only) and DEC-065 explicitly carried forward unchanged a second time (Cyber Lime reskin, in-app only) — both reskins deferred light-mode/email tokens pending a follow-up decision; this is that decision, prompted by the user noticing the just-repasted (DEC-072) auth templates still rendered in the original Ledger Dark blue (#1E40AF) against the app's current Cyber Lime look. Retires the single "Accent (light)" token (05_Design_System) in favor of two: **Accent Fill (light)** (#A3E635, the raw Cyber Lime Primary hex, button backgrounds only) and a new **Accent Text (light)** (#4D7C0F, a darkened same-hue derivative, wordmark/text use only) — the raw hex fails WCAG AA as text on white (~1.9:1 contrast, nowhere near the 4.5:1 minimum), so a text-safe derivative was needed rather than reusing the dark-mode value directly; the derivative reaches ~5:1. Button text on Accent Fill (light) is Text Primary (light) (#0F172A), not white, mirroring the in-app Primary button's own dark-text-on-lime accessibility rule (DEC-065) rather than inventing a new one. Neutral light-mode tokens (Page, Surface, Border, Text Primary/Secondary) are unchanged — only the accent-family tokens move. Doc 24's 3 auth templates rewritten to the new colors and re-pasted into Supabase. **Extended (same-day follow-up):** the identical recolor applied to doc 25 (`25_SubSense_Resend_Notification_Templates_v1.2.html`), the reference markup for all 7 real subscription-reminder email types — doc 25 is documentation-only (per its own header, the live send path is a separate hardcoded shell in `_shared/email-template.ts`), so this pass also flagged the matching code change rather than making it directly — the user then ran it via Claude Code in the same session: committed as `39358cd` ("Recolor reminder email shell to Cyber Lime light-mode tokens (DEC-073)," 1 file, colors + comment only) and pushed to `origin/main`, verified `HEAD`/`origin/main` both at `39358cd`, `tsc -b`/`eslint`/`npm run build` all clean. **Live-verified (same-day follow-up):** a real test-sent reminder email confirmed rendering the new colors end to end — Cyber Lime button (#A3E635), #4D7C0F wordmark — not the old Ledger Dark blue. Closes the one remaining verification step this decision had left open. | Using the raw dark-mode Primary hex (#A3E635) directly as light-mode text/wordmark color was considered and rejected outright — it visibly fails contrast on white, an accessibility regression, not a style choice. Reusing the dark-mode Secondary Accent (#38BDF8, "Cool Steel Blue") for light-mode text instead of a lime derivative was considered and rejected — DEC-065 deliberately keeps that blue exclusive to the wordmark's rare gradient treatment in-app; extending it to become the everyday light-mode accent color would undermine the same restraint DEC-065 built that scoping decision on. | The gap was known and explicitly deferred twice (DEC-057, DEC-065) rather than an oversight found today — resolved now because the user directly noticed the mismatch while re-pasting DEC-072's templates, and the underlying question (does light-mode email get Cyber Lime, and what's the contrast-safe way to do it) needed answering rather than deferring a third time. Extending to doc 25 in the same pass keeps both light-mode/email surfaces (auth and reminder) consistent rather than leaving one resolved and one stale. | 05, 24, 25 | Active |
| DEC-074 | UX | Designs, but explicitly does not ship, a logo-icon-plus-text header treatment for all light-mode/email templates: the real logo icon (`src/assets/subsense-logo-icon.png`, DEC-060) alongside the existing text wordmark, not replacing it, in a small nested `<table>` (icon cell + text cell, not flex/inline-block, for Outlook compatibility), image hosted at a fixed public URL (`https://subsense.co.in/logo-icon.png`, requiring the PNG be copied into Subsense-web's `public/` folder rather than referenced via its normal Vite-hashed build path). **Decision (same-day follow-up): not implemented during the capstone phase.** The user, after reviewing the exact scope (a `public/` asset copy plus edits to doc 24, doc 25, and the live `_shared/email-template.ts` code), decided this is worth doing only if SubSense continues past capstone as a real product — deliberate MVP scope discipline, not a rejection of the idea itself. Doc 24 (v1.4) and doc 25 (v1.3) both record the exact markup as a reference note in their own Version History, but their actual template bodies stay text-only, matching what's genuinely live in Supabase and in `_shared/email-template.ts` — neither was ever pasted or edited for this. | Replacing the text wordmark with the icon alone was considered and rejected regardless of the shipping decision — a real, common failure mode (images blocked by default in Outlook) would leave the header blank for a meaningful share of recipients, worse than today's text-only header. Shipping it now anyway, since the design work was already done, was considered and rejected — MVP scope discipline applies to effort already spent designing something just as much as effort not yet spent; a capstone project's remaining time is better spent on core functionality than a trust-signal polish item with no functional gap behind it. | User-requested, drawing a direct comparison to how established consumer brands (Amex, Zomato) use a recognizable logo mark in transactional email headers as a trust/anti-phishing signal — a legitimate point, and one that brings the email templates in line with DEC-060's already-decided real-logo-asset direction. Deferring the actual implementation, rather than the idea, respects both the good idea and the project's capstone-scope reality. | 24, 25 | Active |
| DEC-075 | UX | Closes the last open item DEC-072 flagged and NEXT_SESSION_AGENDA.md carried forward across several sessions: whether to draft a branded 4th template for Supabase's "Password changed" Security notification (a dashboard section distinct from the Templates tab TEMPLATES 1-3 live in, requiring its own project-level enable toggle). Decision: yes — drafted TEMPLATE 4 in doc 24 (v1.5), styled identically to TEMPLATES 1-3 (white card, IBM Plex Sans, #4D7C0F wordmark, #A3E635/#0F172A button, per DEC-073's tokens). Structurally different from TEMPLATES 1-3 in one respect: this notification is informational (the password change already happened) rather than a confirm-action flow, so it has no `{{ .ConfirmationURL }}` — the CTA instead links to Subsense-web's own forgot-password route via `{{ .SiteURL }}/auth/forgot-password` (confirmed live at that path in `App.tsx`'s router), labeled "Wasn't you? Reset password." Subject line matches Supabase's own default, "Your password was changed," rather than inventing new copy. This is a docs-only deliverable this pass — the user still needs to paste the block into Security notifications (not Templates) and enable the notification toggle before Supabase will actually send it. | Leaving it as Supabase's plain default, matching the original DEC-072 lean toward change-email, was considered and rejected this time — unlike change-email (a rare, already-authenticated action), a password-changed alert is a security-relevant message users are primed to scrutinize for legitimacy, exactly the kind of email an unstyled generic template undermines trust in. Reusing `{{ .ConfirmationURL }}` by pointing it at a generic reset flow was considered and rejected — this notification type doesn't populate that variable at all (confirmed via Supabase's own template-variable reference), so the CTA had to resolve some other way; linking to the app's real forgot-password route was more useful to a genuinely-compromised user than no link at all. | User-requested as a direct follow-up to DEC-073's color fix and DEC-072's original change-email scoping call, closing out the one password-changed-template decision NEXT_SESSION_AGENDA.md had listed as still open for multiple sessions. | 24 | Active |
| DEC-076 | Technical | Discovered and fixes a real, previously-undetected production bug found while the user was live-testing DEC-075's new TEMPLATE 4: clicking its "Wasn't you? Reset password" CTA (`{{ .SiteURL }}/auth/forgot-password`) returned a 404. Root-caused, not assumed: Subsense-web's frontend is a pure client-side React Router SPA with no server-side routes, and no `vercel.json` exists anywhere in the repo — Vercel's zero-config Vite build serves only the files that physically exist in the build output, so a direct browser navigation (any external link, including an email link) to a nested client-routed path like `/auth/forgot-password` 404s before React Router ever runs; only in-app `<Link>` navigation ever worked, since that never leaves the already-loaded page. Confirmed this is not cosmetic or isolated to the new template: `AuthContext.tsx`'s real `resetPasswordForEmail()` call sets `redirectTo: `${window.location.origin}/auth/reset-password`` — the exact same broken path pattern — meaning the actual production "forgot my password" flow was very likely 404ing for any real user clicking that email link, not just DEC-075's new CTA. This had gone undetected because the one prior "confirmed working end to end" test of this flow (doc 19's DEC-era build note) ran against local dev, before the Vercel deployment (doc 19 v1.38) existed to expose it — confirm-signup/email-change links never surfaced this, since Supabase's own redirect for those lands on the root `/`, which always resolves regardless of rewrite config. Fix: added `vercel.json` at the repo root with a catch-all rewrite (`{ "source": "/(.*)", "destination": "/index.html" }`) so every path resolves to the SPA shell and React Router takes over client-side, same as the standard fix for any client-side-routed SPA on Vercel. Doc 15 (Deployment Architecture) updated with a new SPA Routing note recording this requirement, since it wasn't previously documented anywhere. **Shipped and confirmed live (same session):** the user's Claude Code session committed `vercel.json` (commits `0c26ee0`, `c35aaef`), pushed to `origin/main` (`HEAD` == `origin/main` confirmed), and Vercel redeployed. Live-verified directly (typed URL, not in-app navigation): `/auth/reset-password` now renders instead of 404ing, and DEC-075's "Wasn't you? Reset password" CTA (`/auth/forgot-password`) also confirmed working. The real end-to-end recovery flow (clicking Supabase's actual password-reset email link, landing on `/auth/reset-password`, submitting a new password) was also confirmed working by the user. One transient `AuthSessionMissingError` ("Auth session missing!") occurred on a first attempt before a retry succeeded with no code changes in between — most likely a single-use recovery token already consumed (e.g. by an email client's link-prefetch/security scan, a documented Supabase gotcha) rather than a routing or session-exchange defect, since the identical flow succeeded moments later unchanged; noted here rather than silently dropped, but not treated as a live, reproducible bug absent a repeat occurrence. | Adding a dedicated server-side route/redirect page instead of a blanket SPA rewrite was considered and rejected — there's no server in this stack (pure static Vercel deployment), and a catch-all rewrite is the standard, minimal fix for exactly this class of bug on any SPA host; no reason to invent something more complex. Leaving it until the next real user hit it in production was considered and rejected — this is a core auth-recovery path (a user who forgot their password and can't get back in), not a cosmetic or rarely-hit flow; a silent 404 there is a genuine account-lockout risk, not deferrable polish. | Found only because DEC-075's own live test happened to exercise a path (`/auth/forgot-password`) that had never been directly tested against the production Vercel deployment before — the same category of "real drift caught by actually testing, not assuming" this project's citation and live-verification passes exist to catch, just for infrastructure instead of documentation this time. | 15 | Active |
| DEC-077 | Product | Confirmed, via a full codebase grep for `signInWithOtp` (Supabase's magic-link/OTP sign-in call), that Magic Link authentication was never actually implemented as an in-app sign-in method — zero references anywhere in Subsense-web. DEC-011 only ever locked in Google Sign-In as primary (email/password added later, per doc 19's build history); Magic Link was never separately scoped as a real auth path, despite doc 24's TEMPLATE 2 styling Supabase's default Magic Link email template as if it were live. Surfaced when the user, live-testing DEC-075/doc 24's templates, correctly identified they had no way to trigger or test it through the app. **Decision: explicitly defer scoping real Magic Link sign-in until/unless SubSense continues past capstone as a real product** — same deliberate MVP scope discipline as DEC-074's logo-icon deferral, not a rejection of the idea. Doc 24's TEMPLATE 2 is left as-is (still styled, still documented) but is now flagged in its own header comment as describing a template Supabase renders by default that nothing in the app currently sends — a known, deliberate gap, not an oversight the next reader should mistake for a bug. | Implementing real Magic Link sign-in now, since the email template already exists, was considered and rejected — a capstone project's remaining time is better spent elsewhere than adding a second passwordless auth path when email/password and Google already cover sign-in; this mirrors DEC-074's own reasoning almost exactly (effort already spent on a template doesn't obligate shipping the feature behind it). Removing/blanking TEMPLATE 2 from doc 24 entirely, since it's unused, was considered and rejected — it costs nothing to leave styled and documented, and having it ready (matching the other 3 templates' branding) removes one step if Magic Link is ever scoped for real later. | User-caught while testing, not found by any audit — the same pattern as DEC-072's change-email discovery (a documented template whose actual implementation status had never been directly checked, and turned out not to match assumption). | 24 | Active |
| DEC-078 | Product | A careful, evidence-based audit of Phase 1-6 completion (checking actual files/migrations/code against doc 16's stated acceptance criteria, not trusting doc 19's self-reported status alone) found one real, previously-unflagged gap: doc 16 Phase 6 names "Implement Send Reminder Now in Developer/Test Utilities" as an explicit goal and deliverable, but no such page, route, or component exists anywhere in Subsense-web's frontend — confirmed via a full-repo grep (zero matches for "utility"/"developer" in any relevant sense) and a full review of `App.tsx`'s route list. The underlying single-send capability is real and already works: `send-reminder-email`'s `{ reminder_id }` dev-trigger mode has been directly invoked and live-verified multiple times this same session — the gap is specifically the missing in-app UI surface, not the capability behind it. **Decision: defer building this UI until Phase 11 (Developer/Test Utilities)**, where doc 16 already groups it alongside Test AI Response, Test Email Payload, Test Razorpay Payment, and View integration status as one coherent panel. Building it now, in isolation, would mean either a throwaway single-utility page rebuilt later, or a premature partial Phase 11 built before Phase 7 (AI) and Phase 10 (Razorpay Test Mode) exist to give two of its five utilities anything real to test against. Doc 19's Phase 6 status corrected from an unqualified "functionally complete" to explicitly name this one deferred deliverable, rather than let the gap stand silently glossed over. | Building just this one utility now was considered and rejected — the underlying capability already works and is live-verified, so nothing user-facing or functionally real is blocked; the only cost of deferring is cosmetic (no dev convenience button yet), while the cost of building it now is rework (a one-off page that gets restructured once the other four utilities are ready). Leaving the doc 16 gap unflagged and letting "Phase 6 functionally complete" stand as written was considered and rejected — this project's own convention is to flag every real drift between documented and shipped state rather than silently absorb it, the same standard DEC-072 and this session's other findings were held to. | Found only because the user explicitly asked for a careful, evidence-checked Phase 1-6 review rather than a status-bullet summary — the same category of real gap a surface-level audit would have missed, since the reminder engine's core functionality is genuinely solid and doc 19's narrative reasonably called it "functionally complete." | 19 | Active |
| DEC-079 | Architecture | Formalizes Phase 7's (AI Decision Support) runtime architecture, closing the gap where doc 11's `ai-generate-insight` contract (request/response/error shape) and doc 10's `ai_recommendations` schema were both fully speced but nothing had decided when the endpoint actually gets called, how many subscriptions a workspace-scope batch covers, or which OpenAI model to use — the same category of gap DEC-068 closed for Phase 6 before any Edge Function code was written. **Trigger model: lazy-generate, not scheduled.** The frontend reads the latest `ai_recommendations` row per subscription directly via Path A (owner-select, matching the existing `(user_id, subscription_id, generated_at DESC)` index); `ai-generate-insight` (Path B) is only called when no row exists yet, the existing row is older than a 24-hour staleness window, or the subscription's `next_renewal_date`/`lifecycle_status` has changed since `generated_at` — plus a manual "Regenerate" action on both Subscription Details and the Decision Workspace AI Insight slot. No cron job or scheduled generation is introduced; Phase 6's Supabase Cron precedent (DEC-068) is deliberately not reused here, since insight generation is read-triggered by user navigation, not a time-of-day event. **Workspace batch scope:** Decision Workspace's AI Insight slot calls `{scope: "workspace"}` for at most the 3 subscriptions currently in Critical or Upcoming renewal urgency (reusing `computeRenewalUrgency`, DEC-054) — not all active subscriptions — keeping the batch short, high-signal, and consistent with DEC-045's variety rule (a wall of near-identical cards would read as templated). Subscription Details always requests a single insight for its own subscription regardless of urgency tier. **Model choice:** `gpt-4o-mini`, recorded in `model_version` per row for reproducibility — chosen for cost (supports frequent lazy regeneration without a meaningful cost ceiling) and because the AI Copy Tone rules (DEC-045: short, factual, varied phrasing, no manufactured urgency) don't require frontier-model reasoning. **Duplicate subscription detection, deferred to Phase 9:** named in doc 18's PRD pillar copy ("What We're Building") but never actually part of doc 16's Phase 7 acceptance criteria — confirmed a real, previously-unflagged doc 18-vs-doc 16 scope gap, not a build omission. Structurally different from the single-subscription insight loop this decision scopes (`ai-generate-insight` reasons about one subscription at a time; duplicate detection needs to compare across a user's whole active list, and no schema/equivalence-mapping for "these two catalog entries overlap" exists anywhere in doc 10) — building it into this pass would mean inventing that data model against no locked spec. Doc 16's Phase 9 (Insights) goals updated to explicitly name duplicate/overlap detection as a deliverable there, since "show savings opportunities where available" is already Phase 9's own goal and duplicate spend is fundamentally a savings signal — this closes the gap by relocating it to a phase that already owns this kind of insight, rather than leaving it silently dropped. **Resolved during implementation planning (same-day follow-up):** handing this decision to Claude Code for a detailed, file-by-file technical plan (planning only, nothing built yet) surfaced two real contract gaps this decision hadn't specified — both confirmed with the user before the plan was finalized, not silently assumed. (1) **Batch response shape:** doc 11 §5.1's `ai-generate-insight` response was documented as a single flat `{recommendation, reason, financial_impact}` object — mathematically incompatible with a workspace batch covering up to 3 subscriptions. Resolved: single-subscription requests keep the documented flat shape unchanged; `{scope:"workspace"}` requests return `{"insights": [{subscription_id, recommendation, reason, financial_impact}, ...]}`, mirroring this repo's own Phase 6 precedent (`send-reminder-email`'s response already varies between batch and single mode). Doc 11 bumped to v1.11 with the array shape added. (2) **AI Insights/Recommended Reviews merge:** DEC-067 left these as "one concept split across two UI slots" without specifying merged-vs-split; doc 06's C-003 spec has no "primary insight vs. recommended reviews" distinction, only one card per subscription — confirmed doc 06 needs no edit, since no second component was ever separately specced there. Resolved: delete both placeholders, replace with one merged "AI Insights" section rendering up to 3 AI Decision Cards. Doc 04 bumped to v1.20 to state this directly instead of leaving the two-slot framing standing. (3) **Staleness-rule proxy:** this decision's trigger condition ("`next_renewal_date`/`lifecycle_status` changed since `generated_at`") assumed a snapshot comparison, but `ai_recommendations` stores neither field, and altering the append-only table is out of scope for this pass. Resolved: `subscriptions.updated_at > ai_recommendations.generated_at` is used as a conservative superset proxy — the table's existing generic `touch_updated_at` trigger already bumps `updated_at` on any column change, so this occasionally regenerates slightly more eagerly than the literal rule requires (e.g. an unrelated field edit), never less eagerly. No schema change, no doc 10 update needed. None of these three change the trigger model, batch scope, or model choice already locked above — they fill in implementation-level detail this decision hadn't reached yet. Plan reviewed and approved; build completed, deployed, and live-verified (see below). **Further resolved during live verification (same-day extension):** single-subscription AI Insight generation is suppressed for Paused subscriptions — `useAiInsight` shows a static "insight resumes when this subscription is active again" message instead of calling `ai-generate-insight`, no OpenAI call made. This extends DEC-064's existing paused-deprioritization pattern (Paused already excluded from urgency, Upcoming Renewals, and Recommended Reviews) to this new surface, rather than leaving Paused as an accidental exception via "single mode ignores urgency tier." Frontend-only change, no endpoint/contract impact. Archived-subscription suppression (added during the build itself) was live-verified separately: visiting an archived subscription's details page shows the same neutral state with no OpenAI call, and Regenerate/Try again are both correctly locked out. **Paused-subscription suppression, initially only code-traced (no browser available in that sandbox), now live-verified directly by the user:** visiting a Paused subscription's details page shows the static "Insight resumes when this subscription is active again" message, no button, no OpenAI call. Both suppression states (Archived, Paused) are confirmed live, closing this decision out completely. **Further resolved (same-day extension): auto-regenerate cooldown.** The user identified a real residual risk in the `updated_at`-based staleness proxy above: since Paused suppression only blocks calls *while* paused, resuming (lifecycle_status -> active) still bumps `updated_at` via the generic `touch_updated_at` trigger, so a resume followed by a page visit always looked stale regardless of how recently a real insight had already been generated — repeated pause/resume cycling could trigger repeated real OpenAI calls with no cap. Fix: a `MIN_AUTO_REGENERATE_INTERVAL_MS` (5 minutes) floor added to `shouldRegenerateInsight()` in `ai-insight-utils.ts` — if `generated_at` is within the last 5 minutes, auto-regeneration is skipped regardless of the `updated_at`/24h checks, which are otherwise untouched. Scoped to the silent/auto-trigger path only; manual "Regenerate" was confirmed structurally exempt (both hooks' `regenerate()` call `generate("manual", ...)` directly, never through `shouldRegenerateInsight`), so no explicit bypass logic was needed. Live-verified: pausing then resuming a subscription within 5 minutes no longer triggers an OpenAI call; a manual Regenerate click still does, as intended. One false-positive repro during testing (resume-under-5-minutes still appearing to call OpenAI) was traced via temporary instrumentation and ruled out as a stale pre-rebuild bundle, not a logic bug — the debug logging was committed, reproduced clean, and reverted (`useAiInsight.ts` confirmed byte-identical to its pre-debug state via `git diff`). Accepted as sufficient for MVP scope; a "true" fix distinguishing genuinely content-relevant changes from a bare lifecycle toggle would need the snapshot-tracking schema change this decision's staleness-proxy resolution already ruled out above — revisit only if real-world cost/frequency becomes a problem. | Scheduled/pre-generated batch (a daily cron job pre-computing insights for all users, mirroring Phase 6's DEC-068 pattern) was considered and rejected — Decision Workspace/Subscription Details are read on unpredictable user schedules, not a fixed daily cadence, so a cron job would either run far more often than needed (cost) or leave insights stale for users who log in outside its window; lazy-generate-on-read with a staleness window achieves the same "usually cached, rarely live-called" outcome without a scheduler. Generating an insight for every active subscription on every Decision Workspace load was considered and rejected as both expensive and repetitive against DEC-045's own variety rule; a single "one priority pick" was also considered and rejected as too thin against Recommended Reviews' existing framing as a short list, not one card. `gpt-4o` was considered and rejected for this phase — no evidence yet that `gpt-4o-mini`'s output quality is insufficient for AI Copy Tone's requirements, and the cost difference matters directly given lazy-regeneration's higher call volume than a single nightly batch would have had. Including duplicate detection in this pass was considered and rejected per the reasoning above — the user asked directly whether deferring created any real challenge, and the answer (a structurally different, unscoped capability) held up as a genuine reason to defer, not just schedule pressure. | User-directed: asked to plan Phase 7's architecture before handing Claude Code a detailed technical plan, mirroring the DEC-068 precedent of settling platform/shape decisions before Edge Function code gets written. The duplicate-detection scope question was raised proactively (found via a documentation cross-check, doc 18 vs. doc 16) rather than silently built or silently dropped, consistent with this project's standing convention of flagging real doc-vs-doc and doc-vs-shipped gaps rather than assuming. | 04, 11, 16 | Active |
| DEC-080 | Architecture | Formalizes four Phase 8 (Shared Subscriptions) architecture forks left open by doc 10's already-locked schema (`shared_subscriptions`/`shared_members`/`payment_requests`) and doc 11's already-specced `send-shared-payment-reminder` (§5.3) — resolved via direct interview with the user before any DEC-level detail or Claude Code hand-off, mirroring the DEC-068/DEC-079 precedent of settling forks before a build prompt gets written. **(1) Payment-request generation:** a new DB trigger, extending DEC-070's own pattern (which already reacts to `subscriptions.next_renewal_date` changes to cancel/regenerate reminders), fires on the same event and creates one `payment_requests` row per active `shared_members` row for the new `billing_cycle_date`, snapshotting `amount` from `shared_members.amount_owed` at that moment — not a lazy-generate-on-page-load approach (DEC-079's own Phase 7 precedent) and not a daily scheduled Edge Function (DEC-068's Phase 6 precedent). Chosen over both because this is money-owed tracking, not a cosmetic insight: a request must exist the instant a cycle rolls over regardless of whether anyone visits the Shared Subscriptions page or a cron tick has run, since `send-shared-payment-reminder` (already specced) needs something real to attach to. **(2) Share entry point:** a new "Share this subscription" action added to `SubscriptionDetailsPage`, alongside the existing Paid/Paused/Archive quick actions (DEC-064) — not a step folded into Add/Edit Subscription, not a separate flow launched from the Shared Subscriptions landing page. Matches `shared_subscriptions.subscription_id`'s unique-per-subscription schema (sharing is an attribute added to a subscription already being tracked, not an independently-created object) and doc 04's own blueprint, which already lists "Sharing" as related to Subscription Details. **(3) Equal-split computation:** for `split_method = 'equal'`, `shared_members.amount_owed` auto-computes as subscription cost divided by active member count, and rebalances automatically whenever a member is added or soft-removed — making good on the enum value's literal meaning rather than treating it as a one-time suggestion. Already-generated `payment_requests` rows are per-cycle snapshots and are never retroactively altered by a later rebalance; only future generation uses the newly rebalanced amount. `custom` split stays fully manual, unaffected by this rule. **(4) Member-removal request cleanup:** removing a member does **not** cascade-cancel any open (`pending`/`paid_pending_confirmation`) `payment_requests` tied to them — left open and unchanged, matching doc 10's own stated rationale for soft-removal ("preserve payment history"). Deliberately different from the existing archive-cascade rule (archiving the parent subscription *does* cancel open requests): archiving means there is no subscription left for anyone to owe money toward at all, a categorically different signal than one member leaving an otherwise-still-active split. **Not resolved by this decision, flagged for the next pass:** component-level UI specs for the Shared Subscriptions page itself (member list row, add/edit-member form, payment-request list item, mark-paid confirm dialog) — doc 06 only has C-006 (Shared Activity Card, a Decision Workspace summary card), not a per-screen spec. Draft these next, then hand Claude Code a detailed technical plan, matching the DEC-068/DEC-079 sequence. **Further resolved (same-day extension): the component-spec gap is closed.** Three new components added to doc 06 (bumped to v1.24): C-019 Shared Member Row (display name/email, amount owed, active-only rendering — a removed member's row drops out of this list per DEC-080's non-cascade rule, though their `payment_requests` history stays visible elsewhere), C-020 Add/Edit Member Form (amount field editable only under `custom` split, read-only computed under `equal` per DEC-080's rebalance trigger; reused for both first-time setup via "Manage sharing" and ongoing management), and C-021 Payment Request Item (actor-scoped actions per DEC-037: owner gets Mark Paid + Send Reminder, linked member gets self-report "I've Paid" only, resolved/cancelled rows show no actions). No new dialog component was needed — C-017 Confirmation Dialog already named "Reminder confirmation" and "Payment status confirmation" as variants (written before Phase 8 was designed, never given concrete behavior); both are now defined against DEC-080's actual transitions rather than left as unused placeholder names. Phase 8 is now fully staged — architecture, schema, API contract, and component specs all locked. **Further resolved (same-day extension): Phase 8 technical-planning gaps closed.** Handing DEC-080 to Claude Code for a detailed technical plan (mirroring the DEC-068/DEC-079 sequence) surfaced two real implementation gaps this decision's text hadn't specified, both resolved with the user directly, plus two accepted tradeoffs neither raised as a fork since each has a single reasonable answer — all four confirmed before the plan was approved. **(1) Initial generation timing:** the trigger in fork (1) above fires only on `next_renewal_date` UPDATE, but first-time sharing setup or a new member joining an already-shared subscription never changes that column — literally as specified, a newly-shared subscription would generate zero `payment_requests` until the next renewal cycle. Resolved: a second trigger, `shared_members_generate_initial_request` (`after insert on shared_members`), calls the same per-member generation helper the renewal trigger uses, targeting the parent subscription's current `next_renewal_date` — one callable helper reused by both trigger events rather than duplicated logic, mirroring DEC-070/file 29's own helper-extraction precedent. **(2) Unlinked-member reminders:** `shared_members.user_id` is nullable by design (a member may be tracked by name/email only, no SubSense account), but `reminders.user_id` and `notifications.user_id` are both NOT NULL, so neither the auto-generated reminder nor the manual Send Reminder action could attach a row for an unlinked member. Resolved: loosen both columns to nullable, each backed by a replacement CHECK requiring `user_id IS NOT NULL OR payment_request_id IS NOT NULL` (mirroring DEC-069's existing `reminders_shared_payment_target` pattern of loosening a target-presence constraint for a specific reminder-type case) — scoped narrowly, every other reminder type still requires `user_id`. Recipient-email resolution in `send-reminder-email` branches: `user_id` set -> `users.email` as today; `user_id` null -> `shared_members.email` via the request's `payment_request_id`. `reminder_history.user_id` was checked directly against the live migration and confirmed already nullable, so it needs no change. **(3) Equal-split rounding remainder (accepted tradeoff, not a fork):** `cost / active_count` will not always divide evenly (e.g. ₹1000/3 -> ₹999.99 total); accepted as a standard MVP simplification, not solved by an owner-adjustable remainder this pass, and recorded with an inline comment at the division itself so it reads as deliberate. **(4) RLS gap on unlinked-member rows (accepted tradeoff, not a fork):** `reminders_select_own`/`notifications_select_own` are both keyed on `user_id = current_app_user_id()`; once `user_id` is null for an unlinked member's row, that row becomes permanently unselectable via Path A by anyone, including the owner. Accepted for this pass since generation/sending both go through the service-role client (bypassing RLS by design) and no locked component spec (C-019/C-020/C-021) surfaces reminder/notification history in the UI — a future audit-trail or "last reminded" feature would need a new owner-scoped SELECT policy (e.g. via `payment_request_id -> shared_subscriptions.owner_user_id`) to read these rows at all. Both tradeoffs are recorded here and via inline comments in the migration itself. Plan reviewed and revised twice (naming collision between the existing card-level `MarkPaidDialog` "Paid" quick action and C-021's new per-member "Mark Paid" action also flagged and resolved via distinct UI copy, not just distinct components, during the same review) and approved; build completed and live-tested (see below). **Further resolved (live-testing pass): three real bugs found testing the built feature, all fixed.** (1) The equal-split formula's divisor never counted the owner as a sharer — with 1 member, that member was billed the full cost instead of half. Corrected to `active_count + 1` (the owner is an implicit sharer, never represented by a `shared_members` row of their own); the accepted-rounding-remainder note above still stands, now against the corrected divisor. (2) A still-`pending` `payment_requests` row froze at whatever the split was the instant it was created, even as later member changes correctly updated `shared_members.amount_owed` itself — confusing when members were added one at a time. Fixed: the same rebalance also syncs any still-`pending` request's `amount` to the current per-member value; once a request moves past `pending`, it is untouched, exactly as before. Both ship via `32_SubSense_Equal_Split_Fixes_v1.0.sql`. (3) The icon-only action buttons on the Shared Member row and Payment Request row (C-019/C-021) had no accessible hover label — fixed with a new `Tooltip` primitive (this codebase's first, built on `@base-ui/react`, matching how every other primitive here is wrapped), reusing each button's existing `aria-label` text so the visible tooltip and accessible name can't drift apart. **Further resolved (live-testing pass): a real, previously-undetected access gap.** `shared_members.user_id` was never populated by the add-member flow at all — confirmed by reading the code directly — so the "linked member: select only" RLS grant documented above could never actually fire for anyone, regardless of whether that member already had a SubSense account. Fixed with a new `before insert` trigger, `handle_shared_member_link_existing_user` (`33_SubSense_Link_Existing_User_On_Member_Add_v1.0.sql`): looks up `public.users` by a case-insensitive email match and sets `user_id` if found. Scoped to add-time only — **accepted, deferred gap:** a member added before they have an account, who signs up later, is not retroactively linked; the user explicitly accepted this for capstone scope, expecting a planned future dev-access tool to mitigate it rather than building retroactive linking now. **Further resolved (live-testing pass): a second, related bug — a linked member's Shared Subscriptions page silently showed nothing.** Root-caused via static trace (not assumed): `current_app_user_id()`, `is_shared_subscription_member()`, and the RLS policy itself were all confirmed structurally correct; the actual cause was `useSharedSubscriptionsList.ts` embedding `subscriptions(...)` inside its `shared_subscriptions` query — since the base `subscriptions` table grants SELECT to its owner only and the FK between the two tables is `NOT NULL`, PostgREST's default embed behaves inner-join-like, silently dropping the entire parent `shared_subscriptions` row whenever the embedded `subscriptions` row is invisible to the caller, even though the parent row's own RLS would have allowed it. Fixed with a new, additive RLS policy, `subscriptions_select_shared_member` (`34_SubSense_Shared_Member_Subscription_Visibility_v1.0.sql`), plus a `subscriptions!left(...)` embed-hint hardening in the same query as defense-in-depth. **Explicit, confirmed scope tradeoff:** the new policy is a full-row grant, matching this schema's existing table+row-only security convention (no column-level security exists anywhere in it) — a linked member also gains read access to `cost`, `payment_method`, and `payment_reference_note` for the shared subscription, not just its name. Considered and accepted rather than inventing this schema's first column-level-security mechanism for one field (`payment_reference_note`, the only genuinely sensitive one of the three) — worth revisiting only if this product ever has real, non-personal users. **Further resolved (live-testing pass): the new grant's blast radius reached further than the one query it was meant to fix.** A linked member could reach `SubscriptionDetailsPage.tsx` directly (previously owner-only in practice) and could see the shared subscription listed under their own "My Subscriptions" (`SubscriptionsListPage.tsx`) and Decision Workspace (`DecisionWorkspacePage.tsx`) — both pages queried `subscriptions` trusting RLS alone to mean "mine," which stopped being a safe assumption the moment a second, additive SELECT policy existed. Fixed: both list pages given an explicit `.eq("user_id", appUser.id)` filter; `SubscriptionDetailsPage.tsx` given an explicit `isOwner`-based redirect-away guard for a non-owner, since this page was always meant to be owner-only (`SharedSubscriptionsPage` is a linked member's real, intended access point, per this decision's own component-spec resolution above). `useSharedSubscription.ts`/`useSharedSubscriptionsList.ts` deliberately left untouched — those intentionally include member access by design, the correct, different case. A full-repo grep of every `.from("subscriptions")` call site confirmed these were the only two vulnerable reads; the AI Insight Edge Function (`ai-generate-insight`) was checked directly and confirmed already correctly owner-scoped on both its reads. **Further resolved (live-testing pass): a related, separately-found defensive-coding bug.** Testing the access-gating fix above surfaced that a write blocked by RLS was being reported to the user as a success toast — Postgres RLS does not raise an error when an `UPDATE`'s `WHERE` clause matches a row the caller can't touch, it silently affects zero rows, and code that only checks `{error}` cannot tell the difference from a genuine success. Audited and fixed every mutation in the codebase following this pattern (8 call sites across 6 files: `SubscriptionDetailsPage.tsx`'s edit-save and archive actions, `SubscriptionCard.tsx`'s pause/resume toggle, `MarkPaidDialog.tsx`'s confirm action, and both `useSharedSubscription.ts`'s and `useSharedSubscriptionsList.ts`'s shared `runMutation` helpers) — not just the one call site (the archive button) that got caught live. Each now adds `.select()` and treats an empty/null result as a failure. **Not yet resolved, carried to `NEXT_SESSION_AGENDA.md`:** a removed member's own already-issued, still-`pending` `payment_requests` row is designed at the RLS level to remain visible to them (`is_shared_member_self()` deliberately has no active-status check, unlike its `is_shared_subscription_member()` sibling, specifically so removal doesn't cut off their access to a request they may still owe) — but `useSharedSubscriptionsList.ts`'s two-step query (fetch visible `shared_subscriptions` rows, then fetch `payment_requests` scoped only to those returned ids) means a removed member loses access to that still-valid request anyway, since they can no longer see the parent share at all once removed. The database was built to preserve this access; the frontend query structure defeats it for this one case. Not fixed this pass. | Lazy-generate-on-page-load for (1) was considered and rejected specifically because it was the wrong precedent to reuse here — Phase 7's staleness tolerance (a few hours of an AI Insight not being perfectly fresh) is a cosmetic non-issue; a payment-request feature silently not existing for days because nobody opened a page is a real "did I get billed correctly" gap. A daily scheduled Edge Function was considered and rejected as redundant complexity (a second Cron job) for something a same-transaction DB trigger already handles instantly and reliably, at the cost of up to 24 hours of lag in the worst case. Folding the Share entry point into Add/Edit Subscription was considered and rejected — it would add a branch to the common case (most subscriptions aren't shared), making the everyday quick-add flow heavier for a minority use case. A dedicated "New Shared Subscription" flow from the landing page was considered and rejected — it doesn't actually save a step (still needs an existing-subscription picker) and fragments subscription management across two pages instead of one. Freezing the equal split at creation time (no rebalance) was considered and rejected — it would let "equal" silently become mathematically wrong the moment membership changes, defeating the purpose of the enum value existing at all. Auto-cancelling a removed member's open request was considered and rejected — it would have the system silently make a financial-forgiveness decision as a side effect of a membership-management action, which is a materially different and riskier assumption than the archive-cascade case. | User-directed: asked to think through and present a recommendation with a trade-off for each of the four forks, then interviewed directly rather than assumed — same standing discipline as DEC-068/DEC-079's own architecture-forks-before-build sequencing, applied here even though most of Phase 8's groundwork (schema, API contract, blueprint) was already locked from earlier sessions. **Resolved (this pass):** all three deferred test items passed live — renewal-rollover generation (a new `payment_requests` row per active member, correct `amount` snapshot, prior cycle's row untouched), the unlinked-member reminder (delivered to a `shared_members` row with no linked account, confirmed via the `owed_to`/email fallback chain, DEC-081 unblocked delivery), and invalid-transition rejection (a direct `paid` -> `pending` reversal attempt was correctly blocked by the `validate_payment_request_transition()` trigger, and cross-member visibility independently blocked acting on someone else's request in the first place). The one open decision carried forward — a removed member's own still-pending request staying invisible to them once they lose visibility into the parent share, despite RLS permitting it — is now resolved: **accepted as a permanent, documented limitation for capstone scope**, not fixed. Real-world removals are expected to happen after settling up in the common case, and the owner's own view/actions on that request are unaffected either way; revisit only if SubSense continues past capstone. Phase 8 is now fully closed. | 04, 06, 08, 10 | Active |
| DEC-081 | Technical | Found and fixed a Phase 6 regression affecting all reminder delivery, not just Phase 8: `send-reminder-email` and `generate-scheduled-reminders` (Cron-only Edge Functions, authenticating via `requireServiceRole()`'s exact match against `SUPABASE_SERVICE_ROLE_KEY`, never a real JWT) were being rejected by Supabase's platform-level JWT verification gateway — every redeploy via `.github/workflows/deploy-edge-functions.yml` (`supabase functions deploy` with no `--no-verify-jwt` flag, no `supabase/config.toml` to override it) silently re-enabled the platform default (`verify_jwt = true`) on both functions, blocking the Cron jobs' service-role-key header before either function's own code ever ran. Confirmed live via `net._http_response`: `401 UNAUTHORIZED_INVALID_JWT_FORMAT` on every hourly tick. Fixed with a new `supabase/config.toml` scoping `verify_jwt = false` to just these two functions (confirmed via grep that `ai-generate-insight` and `send-shared-payment-reminder` both use `requireAuthenticatedUser` — real browser JWTs — and correctly keep the default). A second, independent issue surfaced once the gateway fix deployed: the Cron jobs' stored Authorization header had the service-role key wrapped in literal angle brackets (`<sb_secret_...>` instead of `sb_secret_...`, off by exactly 2 characters) — root-caused via a temporary presence/length/prefix-only diagnostic log (never exposing the actual secret) added to `requireServiceRole()`, then reverted once confirmed. Both fixes verified live: `net._http_response` now shows `200` on the hourly tick, and a real shared-payment reminder was confirmed delivered end to end. | None (deployment/infra fix, no doc 00-26 schema/architecture/API-contract content changed) | Active |
| DEC-082 | Product | Locks in a real gating rule for premium, replacing what had been a technical demonstration with no actual product effect: confirmed via a full-codebase grep that `is_premium` currently gates nothing anywhere in the built product (zero references in `src/` or any Edge Function), despite the Razorpay Test Mode flow and `user_profiles` entitlement schema (DEC-038) already existing. Free-tier users now get exactly 1 AI insight on Decision Workspace (the single most urgent subscription, hard-capped, no locked-teaser cards for the rest). Premium unlocks the full 3-subscription workspace batch (DEC-079's architecture, unchanged) plus the entire Phase 9 Insights page (spend summaries, duplicate/overlap detection, AI insight summary) once built. Also resolves a real doc-vs-roadmap gap found while discussing this: "suggest lower-cost alternatives" (listed in doc 01's AI Strategy as a core AI responsibility) was never actually assigned to any roadmap phase — folded into Phase 9's Insights page scope, premium-gated alongside the rest, rather than left permanently unscoped. Decision recorded only — the actual `is_premium` gating logic (capping the Decision Workspace batch call) is not yet implemented, per the user's explicit choice to decide now and build later, matching the same sequencing already used for Phases 6-8. | 01, 04, 16 | Active |
| DEC-083 | Product | Records Phase 9+10 (Insights, Premium Gating, Razorpay Demo Purchase) as implemented, closing DEC-082's "decision recorded only, gating logic not yet built" status. Built and deployed this session: the Profile page's real Plan Comparison Card and Upgrade flow (`razorpay-create-order`/`razorpay-verify-payment` Edge Functions, doc 11 Section 5.5/5.6), Decision Workspace's real `is_premium` gating (free tier hard-capped to 1 AI insight, premium keeps the existing 3-subscription batch, DEC-079's architecture unchanged, plus an inline muted note explaining the cap to free-tier users rather than a locked-teaser card, matching DEC-082's no-partial-teaser rule), and the Phase 9 Insights page (spend summaries, duplicate/overlap detection, an intra-portfolio cost comparison, and one AI-written summary paragraph over the deterministic figures, premium-exclusive). The idempotent payment-verification approach (a compare-and-swap conditional update plus a self-healing retry, keyed on the transaction's own immutable `verified_at` rather than wall-clock time, to prevent a resent success callback from repeatedly extending `premium_expires_at`) and the free-tier inline cap note were both decided during the build's plan-review rounds, not pre-decided by DEC-082 itself. Two new error codes recorded for doc 11's PAY error family: PAY_004 (plan not found, `razorpay-create-order` — kept distinct from PAY_002's existing "order not found" meaning in `razorpay-verify-payment` rather than overloading one code across two functions) and PAY_005 (upstream Razorpay/DB failure). `user_profiles.is_premium`/`premium_expires_at`/`premium_source` (DEC-038) has real readers for the first time. Three real issues were found and fixed across three plan-review rounds before any code was written: a receipt-length overflow against Razorpay's 40-character limit; the existing `payment_transactions_scrub_signature` trigger silently nulling the signature column on write (accounted for, not worked around); and, the most serious, the original self-healing logic recomputing `premium_expires_at` from `now()` on every duplicate verification callback, which would have let a resent success payload extend a paid user's premium indefinitely — fixed by keying the computed expiry off the transaction row's own fixed `verified_at`. Recorded here, not left implicit: this phase is implemented but not yet fully closed. Live testing surfaced three unresolved issues tracked in `NEXT_SESSION_AGENDA.md` rather than silently deferred: the Insights page's AI-written summary intermittently omits one of two currency totals it was given (non-deterministic, since the page computes fresh with no caching); a full-page browser reload on in-app navigation, confirmed genuine but not yet root-caused (the routing code itself, `App.tsx`/`Sidebar.tsx`, was checked directly and found correct); and the app has no session-idle-timeout policy at all today, which the user wants given the app now handles payments. The Cost Comparison Card's shipped label, "Lower-Cost Alternatives," was also flagged as overpromising what an intra-portfolio category-average comparison actually delivers, with no real market-pricing data anywhere in this schema to justify a stronger claim — a rename and copy rewrite were proposed but not yet built. | 04, 06, 10, 11, 16 | Active |
| DEC-084 | Frontend | **Corrects DEC-083's "not yet root-caused" status** for the full-page-reload-on-navigation bug with a confirmed, evidence-based root cause, and records the fix. DEC-083 left this open after `App.tsx`/`Sidebar.tsx` were checked directly and found correct — that check was accurate but incomplete: live reproduction this session (narrowed by the user's own precise testing, not assumed) established the reload never fires from in-app navigation (sidebar clicks, browser back/forward) at all, only after switching to another browser tab or app and returning. Root-caused by reading `@supabase/auth-js`'s shipped source directly (`node_modules/@supabase/auth-js/dist/main/GoTrueClient.js`), not assumed: `GoTrueClient` registers a `visibilitychange` listener: on every tab-regains-visibility event (not just once per token lifetime), `_onVisibilityChanged` -> `_recoverAndRefresh()` re-notifies every `onAuthStateChange` subscriber with `SIGNED_IN` (session not near expiry — confirmed the common case, `GoTrueClient.js:4082`) or `TOKEN_REFRESHED` (session near expiry). `AuthContext.tsx`'s listener ran the full `applySession()` reset — `setLoading(true)`, a full refetch of `users`/`user_profiles`/`user_preferences`, `setLoading(false)` — unconditionally for every event it received, and `ProtectedRoute.tsx` renders a full-screen loading spinner over the *entire* app tree whenever `loading` is true. So every tab refocus blanked the whole app; it was never a route remount, a Fast Refresh artifact, or a real browser navigation (the already-completed diagnostic step had separately ruled out `location.reload()`, any navigation-on-auth-state `useEffect`, and any service worker). **Fix:** `AuthContext.tsx` now tracks the identity (auth user id, or null) its last successful `applySession()` call actually resolved, via a ref. The `onAuthStateChange` listener compares each incoming event's session's user id against it: unchanged (same user still signed in, or still signed out) means this is Supabase's own background re-notification, not a real identity change — the handler now just updates the `session` token state and returns, skipping the loading blank and the needless refetch entirely. Only a genuine identity change (real sign-in, sign-out, or a different user) still runs the full reset. **Scope note, recorded because it reversed an in-progress approach:** before this root cause was pinned down, a different fix was drafted and partly built — a page-level stale-while-revalidate caching hook (`useCachedResource`), wired into 7 files (Decision Workspace, Subscriptions List, Subscription Details, both shared-subscription hooks, Insights, and the Profile plan-comparison card) — reasoning from the (accurate, but as it turned out non-primary) observation that several pages reset their own local loading state on every route remount. Once live reproduction showed in-app navigation never blanks the app at all, that approach was recognized as solving a different problem than the one actually reported: `ProtectedRoute`'s spinner blanks everything regardless of what any individual page caches underneath it. That work was fully reverted (not shipped) rather than layered on top of the real fix, to keep this change scoped to the actual cause and avoid unreviewed surface area this close to demo day. | None directly edited this pass — docs 10, 11, 16, and 19 each carry status language from DEC-083's own recording ("full-page-reload-on-navigation bug... not yet root-caused") that is now stale and pending a follow-up correction, not made here. | Active |
| DEC-085 | Product | Closes the two remaining open items from DEC-083's live-testing pass: the Insights AI summary's non-deterministic currency-drop, and the Cost Comparison Card's overpromising "Lower-Cost Alternatives" name. **Currency-drop fix (`insights-generate-summary/index.ts`):** the lead sentence stating currency totals is now built server-side directly from the already-computed `spendSummary` via string templating (e.g. "You're spending ₹868.00 and $9.00 across 4 active subscriptions this month," single-currency portfolios using the same pattern with one figure, no dangling "and"). The OpenAI call still receives the figures as context but its own output is restricted to a short qualitative framing sentence only, concatenated after the templated lead — matching doc 11 §5.8's existing rule that the model provides framing, never computes or restates a figure itself. A code-level guard discards the model's returned framing text and substitutes a static fallback sentence if it matches `/\d/` or contains `₹`/`$`, rather than relying on the prompt instruction alone. **Cost Comparison Card rename:** the response key (`lower_cost_alternatives` → `cost_comparisons`), TypeScript type (`LowerCostAlternative` → `CostComparisonItem`), and backend function (`findLowerCostAlternatives` → `findCostComparisons`) renamed together in one commit, since the Edge Function's response shape and the frontend's consumption of it are a single wire contract — a mismatched deploy window between them would silently break the card. Card display name changed to "Worth a Second Look" (doc 06 C-029's own already-proposed direction from DEC-083). Per-item copy reworded from "₹149.00/mo vs. ₹134.00/mo average for Music" to name the specific subscription, its percentage above the category average, and invite the user to review pricing or tier themselves — matching the "intra-portfolio comparison, not a competitor claim" framing DEC-083 and doc 06 C-029 both already established. The AI summary prompt's own language for this signal updated to match, so the model never calls it a "lower-cost alternative" either. Existing Review Subscription/Remind Me Later actions reused — no new "switch to X" CTA, since no competitor-pricing data exists anywhere in this schema to back one. | A prompt instruction alone (telling the model never to restate a figure) was considered and rejected as the sole safeguard for the currency-drop fix — this is the exact bug being fixed, so a code-level regex guard was added to make recurrence structurally impossible rather than just less likely. Inventing a new "switch to X" CTA for the renamed card was considered and rejected — no market/competitor-pricing data exists anywhere in this schema to responsibly suggest a specific alternative, so the existing generic actions (which don't make that claim) were kept. Shipping the rename as two separate commits (Edge Function response key, then frontend consumption) was considered and rejected — a mismatched deploy window between them would silently break the card for any user visiting between the two deploys, so it shipped as one coupled commit instead. | Directly closes two of the three issues DEC-083 recorded as open and tracked in `NEXT_SESSION_AGENDA.md`, plus the previously-proposed-but-unbuilt Cost Comparison Card rename DEC-083 also flagged. Both fixes were committed and pushed this session; live verification (2-currency ×5 regeneration, single-currency regression, a deliberate temptation case, and visual confirmation of the new copy against both an above- and below-average subscription) had not yet been completed as of this entry — not claimed as fully closed. The session's fourth tracked item, idle-session timeout, is untouched, still gated on a code-free plan and a separate go-ahead before any code, per DEC-083's own recording. **Resolved (this pass):** both fixes now live-verified. The Cost Comparison Card rename renders correctly live ("Worth a Second Look," per-item copy naming the specific subscription and its real percentage above the category average, both live-tested items confirmed). The currency-drop fix was confirmed against a real 2-currency portfolio (INR + USD), both totals present and matching the Spend Summary card exactly across multiple reloads (no caching, computed fresh each time), with prior single-currency testing from the original build session also holding. A separate, unrelated bug surfaced during this verification pass, now also resolved: the Insights page crashed outright (indefinite "Loading your insights…" then a blank screen, `Uncaught TypeError: Cannot read properties of undefined (reading 'length')`) because the live Edge Function was still serving the pre-DEC-085 response shape (`lower_cost_alternatives`) while the already-deployed frontend expected `cost_comparisons` — not a code defect (local source was correct and in sync with `origin/main` at `b076918`), but a failed deploy: the "Deploy Supabase Edge Functions" GitHub Action run for this push had failed with "The job was not acquired by Runner of type hosted even after multiple attempts" / "Internal server error," a transient GitHub-side runner-provisioning outage confirmed via the Actions log, not a defect in the workflow file, secrets, or the Supabase CLI command. Fixed by manually re-running the same workflow unchanged (succeeded in 44s); no code or config change was needed. The idle-session-timeout item remains untouched, still gated on a separate plan-approval go-ahead before any code. | 06, 11 | Active |
| DEC-086 | Product | Closes the fourth and final item tracked open from DEC-083's Phase 9+10 live-testing pass: no session-idle-timeout policy existed, a real gap now that the app handles payments. Reviewed with the user via direct interview (per this project's decide-then-build sequencing) before any code: per-tab idle tracking, not cross-tab-synced (accepted limitation — an idle tab can sign out while another is actively in use; the resulting sign-out still propagates normally through Supabase's own cross-tab session sync and `AuthContext.tsx`'s existing `onAuthStateChange` handling from DEC-084, untouched by this decision); a warning toast with a "Stay signed in" action that resets the clock, not a passive notice; and demo-week thresholds of 30 seconds (warning) / 40 seconds (sign-out) rather than a real production-scale value, chosen so the feature is actually demoable live rather than requiring a genuine wait — to be revisited to roughly 4 / 4.5 minutes if SubSense continues past capstone as a real product (the originally-discussed 15/20-minute value was itself judged too lax once weighed against a real demo). Built as `useIdleTimeout.ts` (new hook, mounted once from `ProtectedRoute.tsx` so it covers every authenticated route and never runs on the public `/auth/*` pages) and a shared `isCheckoutOpen()`/`setCheckoutOpen()` module (`src/lib/checkout-state.ts`, deliberately kept outside `AuthContext` since its only consumer is the idle timer's interval tick reading the value at tick time, not reactively — routing it through Context would re-render every auth consumer on every checkout state change for no benefit). The checkout-suspension mechanism is the part that actually matters: Razorpay's checkout is a third-party iframe overlay that emits no activity events on our own document, so a bare early return during checkout would let wall-clock idle time keep accumulating silently and fire the sign-out the instant a long checkout finished — right on top of a successful payment. Fixed by carrying the last-activity timestamp forward by the tick interval while checkout is open, freezing the *measured* idle duration rather than resetting it (a reset would incorrectly hand a fresh full window to someone who genuinely walked away mid-checkout). `UpgradeButton.tsx`'s checkout flag is set before `checkout.open()` and cleared on all three exit paths (`ondismiss`, `verifyPayment`'s `finally`, and the outer `catch` for `checkout.open()` itself throwing) — a stuck-true flag would silently disable the timeout for the rest of the session. Manually live-verified: warning toast fires at 30s, "Stay signed in" resets the clock, real sign-out fires at 40s and redirects to `/auth`, and — the check that actually matters — leaving a Razorpay checkout open past the sign-out threshold does not sign the user out or interrupt the payment. `tsc -b`/`eslint`/`npm run build` all clean; this repo still has no test runner configured, flagged explicitly per this project's standing practice rather than silently skipped. Committed as `d1d6d48`/`d2d83ab` (feature) and a follow-up `44053bb`/`2926411` (demo-week threshold retune plus a real bug fix: the toast copy computed whole minutes and would have silently shown "signed out in about 0 minutes" at these sub-minute values, fixed with a `formatDuration()` helper rendering seconds below a minute and minutes above it), both pushed to `origin/main`. No API contract, schema, or Path A/Path B architecture change — purely a frontend, client-side feature, so no doc 06 (no new reusable component — the warning toast reuses the existing Sonner pattern already wired project-wide) or doc 11 change is needed. | 16, 19 | Active |
| DEC-087 | Product | Closes out Phase 11 (Developer/Test Utilities) and Phase 12 (Testing/QA), planned, built, and tested together this session per the user's explicit request. **Phase 11 built:** a single `dev-utilities` Edge Function with four actions — `trigger_reminder` (Send Reminder Now: synthesizes a due reminder for the caller's own subscription, delegates to the real `send-reminder-email` in its existing `{ reminder_id }` mode; the create path back-dates `updated_at` by 15 minutes so the row is immediately claimable, the same pattern later reused to fix `send-shared-payment-reminder`'s own bug below), `build_prompt` (Test AI Response: dry-run shows the exact assembled prompt with no OpenAI call and no cost; a real-call mode invokes `ai-generate-insight`/`insights-generate-summary` directly, unmodified — per Fork E, production AI functions were deliberately never touched, with prompt-context assembly duplicated inside `dev-utilities` instead, accepting minor drift risk in exchange for zero regression risk to the live Insights feature this close to the demo), `render_email` (Test Email Payload: renders a real `notification_templates` row through the actual `renderEmail()` path into a sandboxed iframe, zero Resend calls, nothing delivered), and `integration_status` (four read-only health checks — Supabase, OpenAI, Resend, Razorpay — via `Promise.allSettled`, never returning a key, key prefix, or raw provider body to the caller). A fifth utility, Test Razorpay Payment, reuses the existing `UpgradeButton`/Test Mode checkout flow exactly rather than a separate payment path. Hidden route (`/dev-utilities`), not linked in Sidebar/TopBar, mounted inside the existing `ProtectedRoute`. **Access restricted mid-session, not part of the original plan:** doc 16's own restriction language only ever specified "authenticated and protected... not normal end-user navigation" (a hidden route), which in practice meant any authenticated user who knew or guessed the URL could reach it. Narrowed to a 3-account allowlist (exact-match, case-insensitive email, not domain- or pattern-based) checked on both sides — client-side (redirect before the page renders anything) and server-side in the Edge Function itself (403, the real security boundary, since a client-only check doesn't stop a direct call to the function's own URL). **Two real bugs found and fixed during this build, both in already-shipped code, neither introduced by Phase 11 itself:** `send-shared-payment-reminder`'s create-new-reminder path never back-dated `updated_at`, so a genuinely new shared-payment reminder (no pre-existing pending row) would 404 against `send-reminder-email`'s own claim-window query — invisible in normal use because the far more common path (a pre-existing pending row already older than the lease) worked fine; fixed by mirroring `trigger_reminder`'s back-dating. `SubscriptionCard` had nested interactive controls (a `div` with `role="button"` wrapping real `<Button>`s for the card's own quick actions) — a WCAG 4.1.2 violation flagged in an earlier accessibility pass and deliberately deferred at the time; restructured so the card container is a plain, non-interactive `div`, the subscription name became the card's single real `<button>` "open" control (a stretched `::after` overlay), and the action row sits above that overlay at a higher z-index so quick actions receive their own clicks independently — no functionality removed, keyboard access preserved and widened (the previous `stopPropagation` workaround was mouse-only). **Phase 12, a deliberately lightweight pass, not a full RTM cycle:** the user directly challenged whether a full Requirements Traceability Matrix added real value given Phases 1-10's own extensive live-testing already covered most of the app; agreed, and Phase 12 was rescoped to the three areas genuinely unverified — one continuous end-to-end run (signup through idle-timeout, never previously run as a single unbroken journey), a targeted RLS/premium-gating spot-check (not a redo of Phase 8's full audit — `shared_subscriptions` visibility and premium gating specifically), and accessibility (WCAG 2.1 AA via axe/Lighthouse, the one genuinely new category — nothing had been checked on this axis before). All nine live tests passed: the 3-account access gate (both directions — an allowlisted account sees the page, a fourth non-listed account is redirected away); Send Reminder Now (real email delivered, `status: "sent"` with a provider message ID); Test AI Response (both dry-run and a real call, valid structured response); Test Email Payload (correct light-mode render, no delivery); Integration Status (all four services reachable — see the Resend fix below); regression on both bug fixes above (keyboard tab-through, click-anywhere-to-open, and independent quick-action clicks all confirmed on Subscriptions List; a genuinely new shared-payment reminder sent without a 404); the full E2E run; the RLS spot-check (a non-member account correctly denied access to another account's shared subscription and payment requests, confirmed via matching a live URL across two real accounts; premium gating held for a real non-premium account); and the accessibility gate (zero Critical/Serious axe/Lighthouse findings confirmed on all four doc 14-named screens — Decision Workspace, Add Subscription, Subscription Details, Shared Subscriptions — after the fixes below). **One real bug found and fixed during Phase 11's own build, confirmed live during Phase 12's testing:** the Integration Status health check reported a working, send-capable Resend API key as `auth_failed`, discovered because a real email had just sent successfully seconds earlier via the same key — root-caused as a key-scope issue, not a broken credential (Resend's send-only keys are correctly rejected on the unrelated `GET /domains` read the health check also performs, and the check discarded the response body needed to tell a scope rejection apart from a truly invalid key); fixed by having the check's Resend interpreter recognize `restricted_api_key` on a 401/403 and report `"Reachable (send-only key)"` instead of a false failure, without forwarding any provider response body to the caller. **Two more real bugs found live-testing Decision Workspace, neither part of either phase's original scope, caught only because Phase 12's testing exercised that screen directly:** the "Shared Payment Activity" section was a hardcoded static placeholder with no query, hook, or type behind it at all (not a query bug — there was nothing to query), confirmed via grep before being built for real as `useSharedPaymentActivity` (mirrors the proven `useSharedSubscriptionsList` RLS-scoped query pattern, resolved per-row against the viewer's own `shared_members.user_id` rather than a single owner/member flag on the whole share, since a user can simultaneously own one share and be a member of another). Once built, narrowed to a 7-day window with day countdowns ("Due in 3 days," "Due today," "Overdue by N days" — overdue rows deliberately kept, not excluded, matching Upcoming Renewals' own "7-days-or-fewer including overdue" convention) and an honest "Nothing owed in the next 7 days" empty state, rather than surfacing every open payment request regardless of date — the unfiltered list is unaffected and stays fully visible on the Shared Subscriptions page. The "Potential Savings" placeholder (doc 16, confirmed as deliberately unbuilt and predating this phase, doc 06 C-007) was hidden from Decision Workspace entirely rather than left visible for the live demo, since an empty "insights will appear here" card reads as unfinished on demo day — UI-only removal, no calculation logic added, and the reusable engine that should eventually power it (`findCostComparisons`, already built for the Insights page's Cost Comparison Card) is named in a code comment at the removal site so a future build doesn't duplicate that logic. Finally, `TopBarActions`' "Add Subscription" button lost its accessible name below the 640px breakpoint (its visible text label is `hidden sm:inline`, not merely visually collapsed, so under 640px the button had no accessible name at all, not a degraded one) — found via a real Lighthouse mobile-viewport scan (Lighthouse audits at a mobile viewport by default, which is exactly why an earlier desktop-width-only accessibility pass had not caught it); fixed with an `aria-label` matching the visible text exactly (WCAG 2.5.3, Label in Name). Because `TopBarActions` mounts once in `AppLayout`, wrapping every authenticated route, this single fix cleared the finding across all four audited screens plus every other authenticated page, confirmed by re-running Lighthouse on all four (100/100 accessibility, zero failing audits, on each). | Alternatives considered and rejected: a full RTM/checklist/UAT cycle for Phase 12, given Phases 1-10's own extensive live-testing already realized most of the bug-catching value a full cycle would provide — rejected in favor of the lightweight three-area approach above, at the user's own challenge. Domain- or pattern-based email matching for the dev-utilities allowlist (e.g. any `@gmail.com` address, or a `startsWith` check) — rejected as either over-granting access to unrelated accounts sharing the same domain or under-matching a real allowlisted address with a different local-part pattern; exact-match against the three specific known addresses is the correct scope for exactly three known users. Widening `requireAuthenticatedUser` to also resolve and return the caller's email (which would make the allowlist check reusable elsewhere) — rejected as unnecessary blast radius, since it would touch five other functions (`ai-generate-insight`, `insights-generate-summary`, both Razorpay functions, `send-shared-payment-reminder`) for a dev-only need; one extra query scoped inside `dev-utilities` itself is the cheaper, lower-risk trade this close to the demo. Building the real "Potential Savings" feature now, reusing the existing cost-comparison engine, instead of just hiding the placeholder — rejected as new scope added this close to the demo rather than a testing-pass fix; hiding it is UI-only polish, building it would have been unplanned Phase 9 work. | 04, 16, 19 | Active |

| DEC-088 | Deployment | Closes out Phase 13 (Deployment) by documenting what actually happened rather than building doc 16's originally-planned staging-\>approval-\>production pipeline. **Reality:** SubSense has deployed straight to production (Vercel for the frontend, GitHub Actions for Supabase Edge Functions, both auto-triggered on push to `main`) since early in the build — no separate staging Vercel project or Supabase instance was ever stood up. Every phase from Phase 6 onward (DEC-068 through DEC-087) shipped this way, each live-verified by direct testing after deploy rather than gated by a formal staging rehearsal or sign-off step. This was never a secret drift — doc 19's own build history documents dozens of live-verification passes against production directly — but doc 16's Phase 13 goals and doc 15's "Developer -> Development -> Staging -> Production" model had never been reconciled against that reality until now. **Rollback, in practice:** no dedicated rollback tooling exists; the de facto mechanism has always been `git revert` on `main` plus the same auto-deploy pipeline running forward again (used, for instance, when DEC-085's live verification found a failed GitHub Actions run and the fix was simply re-running the same workflow, not a formal rollback). This has never been exercised as a genuine incident rollback, only as ordinary iterative fixing. **Verification checklist, in practice:** doc 15's formal checklist (Application startup, Authentication, Protected routes, Database connectivity, Decision Workspace availability, API health, AI/Email/Payment service health, Critical logs) was never run as a single documented pass-fail gate per deployment — instead, each phase's own live-testing (documented per-DEC in this register) informally covered the equivalent ground on a rolling basis. Doc 15 amended (bumped to v1.13) with a new "As-Built Deployment Record" section stating this explicitly, without deleting or rewriting the originally-specified progressive-environment architecture, which stays documented as the not-yet-built target model rather than a false claim of what exists. Doc 16's Phase 13 section (bumped to v1.25) rewritten from an unstarted plan to a built-and-closed record, each goal/deliverable annotated with its actual disposition. | Standing up a real second Vercel/Supabase environment now purely to formalize a staging step was considered and rejected — ten days from the Aug 16 showcase (per `NEXT_SESSION_AGENDA.md`'s own capstone-checklist section), this would add real setup and secrets-duplication risk for a solo capstone project with no functional benefit, given the app has already been safely iterated on in production across seven prior phases using this project's own established live-test-every-change discipline. Leaving Phase 13 marked "not started" indefinitely was also considered and rejected — inaccurate, since production deployment has been continuously happening since Phase 6; the governing docs should reflect what's true, not what was originally planned before reality diverged. | 15, 16, 19 | Active |

## Superseded or Deprecated Decisions

| Deprecated Term or Idea | Replacement | Reason |
| --- | --- | --- |
| Dashboard | Decision Workspace | Better reflects decision-first purpose. |
| Decision Center | Decision Workspace | Controlled vocabulary standardization. |
| Standalone Search module | Contextual search | Scope simplification. |
| Settings as top-level navigation | Profile | Keeps nav focused. |
| Delete user-facing action | Archive | Recoverability and history preservation. |
| Capstone Mode | Developer/Test Utilities | Clearer implementation term. |

## Decision Traceability Links

Traceability chain:

Product decision -> Source document -> Business rule -> Implementation artifact -> Test case -> Deployment verification

Examples:

| Decision | Business Rule | Implementation Artifact | Test Coverage |
| --- | --- | --- | --- |
| AI never acts for user | BR-001 | AI services, frontend copy | AI validation, E2E review flow |
| Auth required for data | BR-002 | RLS, protected routes | Auth tests, RLS tests |
| First login provisions profile | BR-006 | Auth service, users/profile tables | Onboarding flow tests |
| No duplicate profile on returning login | BR-007 | Auth service | Onboarding regression |
| Auth before data load | BR-008 | Frontend route guards, API auth | Protected route tests |

## Decision Statistics

| Category | Count in This Consolidated Register |
| --- | --- |
| Governance | 3 |
| Product | 18 |
| UX | 26 |
| Architecture | 9 |
| Technical | 5 |
| Security | 2 |
| Testing | 4 |
| Deployment | 4 |
| Database | 8 |
| Frontend | 3 |
| Backend | 2 |
| Documentation | 2 |

**Correction (same day, this pass):** this table was reconciled against an actual per-entry tally of all 61 rows in the Active Decision Register below (DEC-001 through DEC-061, no gaps or duplicates confirmed), rather than trusting the accumulated "bumped X statistic from A to B" shorthand each individual DEC entry recorded over time — that shorthand had drifted from ground truth across several categories (Governance, Product, Architecture, Technical, Testing, Database all previously wrong), and two real category labels used throughout the register (Frontend, 3 entries; Backend, 2 entries) were missing from this table entirely, so every previous total undercounted by 5. Corrected the Decision Categories list above to match (added Testing, Deployment, Database, Frontend, Backend; removed "Development," which was never actually used as a label in the register). Not tied to a new DEC — pure data-integrity housekeeping, same as every other citation/count correction in this document's history.

**Further correction (this pass, recording DEC-062/063/064):** the table above now includes DEC-062 (UX) and DEC-063 (UX), bumping UX from 17 to 19, and DEC-064 (Product), bumping Product from 10 to 11 — verified via the same full per-entry tally method as the correction above (64 total rows, DEC-001 through DEC-064, no gaps or duplicates).

**Further correction (this pass, recording DEC-065/066):** the table above now includes DEC-065 (UX) and DEC-066 (UX), bumping UX from 19 to 21 — verified via the same full per-entry tally method as the corrections above (66 total rows, DEC-001 through DEC-066, no gaps or duplicates).

**Further correction (this pass, recording DEC-067):** the table above now includes DEC-067 (UX), bumping UX from 21 to 22 — verified via the same full per-entry tally method as the corrections above (67 total rows, DEC-001 through DEC-067, no gaps or duplicates).

**Further correction (this pass, recording DEC-068):** the table above now includes DEC-068 (Architecture), bumping Architecture from 6 to 7 — verified via the same full per-entry tally method as the corrections above (68 total rows, DEC-001 through DEC-068, no gaps or duplicates).

**Further correction (this pass, recording DEC-069):** the table above now includes DEC-069 (Database), bumping Database from 5 to 6 — verified via the same full per-entry tally method as the corrections above (69 total rows, DEC-001 through DEC-069, no gaps or duplicates).

**Further correction (this pass, recording DEC-070):** the table above now includes DEC-070 (Database), bumping Database from 6 to 7 — verified via the same full per-entry tally method as the corrections above (70 total rows, DEC-001 through DEC-070, no gaps or duplicates).

**Further correction (this pass, recording DEC-071):** the table above now includes DEC-071 (Database), bumping Database from 7 to 8 — verified via the same full per-entry tally method as the corrections above (71 total rows, DEC-001 through DEC-071, no gaps or duplicates).

**Further correction (this pass, recording DEC-072):** the table above now includes DEC-072 (UX), bumping UX from 22 to 23 — verified via the same full per-entry tally method as the corrections above (72 total rows, DEC-001 through DEC-072, no gaps or duplicates).

**Further correction (this pass, recording DEC-073):** the table above now includes DEC-073 (UX), bumping UX from 23 to 24 — verified via the same full per-entry tally method as the corrections above (73 total rows, DEC-001 through DEC-073, no gaps or duplicates).

**Further correction (this pass, recording DEC-074):** the table above now includes DEC-074 (UX), bumping UX from 24 to 25 — verified via the same full per-entry tally method as the corrections above (74 total rows, DEC-001 through DEC-074, no gaps or duplicates).

**Further correction (this pass, recording DEC-075):** the table above now includes DEC-075 (UX), bumping UX from 25 to 26 — verified via the same full per-entry tally method as the corrections above (75 total rows, DEC-001 through DEC-075, no gaps or duplicates).

**Further correction (this pass, recording DEC-076):** the table above now includes DEC-076 (Technical), bumping Technical from 4 to 5 — verified via the same full per-entry tally method as the corrections above (76 total rows, DEC-001 through DEC-076, no gaps or duplicates).

**Further correction (this pass, recording DEC-077):** the table above now includes DEC-077 (Product), bumping Product from 11 to 12 — verified via the same full per-entry tally method as the corrections above (77 total rows, DEC-001 through DEC-077, no gaps or duplicates).

**Further correction (this pass, recording DEC-078):** the table above now includes DEC-078 (Product), bumping Product from 12 to 13 — verified via the same full per-entry tally method as the corrections above (78 total rows, DEC-001 through DEC-078, no gaps or duplicates).

**Retroactive correction (caught this pass, while adding DEC-080's own entry below):** the static table's Architecture count of 8 already silently included DEC-079 (Architecture), bumping from 7 to 8 — but this bump was never narrated as its own correction note the way every other DEC has been, a real gap in this table's own documentation discipline, not a numeric error (a direct per-entry tally confirms 8 Architecture rows — DEC-016, 017, 031, 035, 039, 047, 068, 079 — is correct as of DEC-079, 79 total rows, no gaps or duplicates). Logged here so the narration trail is complete before the next bump.

**Further correction (this pass, recording DEC-080):** the table above now includes DEC-080 (Architecture), bumping Architecture from 8 to 9 — verified via the same full per-entry tally method as the corrections above (80 total rows, DEC-001 through DEC-080, no gaps or duplicates).

**Further correction (this pass, recording DEC-081):** the table above now includes DEC-081 (Technical), bumping Technical from 5 to 6 — verified via the same full per-entry tally method as the corrections above (81 total rows, DEC-001 through DEC-081, no gaps or duplicates).

**Further correction (this pass, recording DEC-082):** the table above now includes DEC-082 (Product), bumping Product from 13 to 14 — verified via the same full per-entry tally method as the corrections above (82 total rows, DEC-001 through DEC-082, no gaps or duplicates).

**Retroactive correction (caught this pass, while adding DEC-083's own entry below):** the prior correction note above narrated Product bumping from 13 to 14 for DEC-082, but the static table's Product cell was never actually edited to 14 at the time — left at 13, a real gap in that pass's own follow-through, not a numeric error in the narration's logic. A direct per-entry tally of every `Product` row in the register (DEC-001, 003, 004, 005, 009, 010, 011, 032, 041, 059, 064, 077, 078, 082 — 14 rows) confirms 14 was the correct count as of DEC-082, before DEC-083 below.

**Further correction (this pass, recording DEC-083):** the table above now includes DEC-083 (Product), bumping Product from 14 to 15 — verified via the same full per-entry tally method as the corrections above (83 total rows, DEC-001 through DEC-083, no gaps or duplicates).

**Further correction (this pass, recording DEC-084):** the table above now includes DEC-084 (Frontend), bumping Frontend from 3 to 4 — verified via the same full per-entry tally method as the corrections above (84 total rows, DEC-001 through DEC-084, no gaps or duplicates). Not re-auditing the rest of the table in this pass — the static Technical cell (5) already appears to lag the DEC-081 correction note above (5 to 6) the same way the Product cell once lagged DEC-082's, but reconciling that is a separate housekeeping pass, not part of recording DEC-084.

**Further correction (this pass, recording DEC-085):** the table above now includes DEC-085 (Product), bumping Product from 15 to 16 — verified via the same full per-entry tally method as the corrections above (85 total rows, DEC-001 through DEC-085, no gaps or duplicates). The pre-existing Technical-cell lag flagged in DEC-084's correction note above is still not reconciled — carried forward, not part of recording DEC-085 either.

**Further correction (this pass, recording DEC-086):** the static table's Product cell was still at 15, not 16 — the DEC-085 correction note above narrated the bump but the table itself was never actually edited, the same gap pattern DEC-084's own correction note flagged for the Technical cell. Fixed both in this pass: Product corrected to 17 (accounting for the missed DEC-085 bump and the new DEC-086), verified via a full per-entry tally of every `Product` row in the register (DEC-001, 003, 004, 005, 009, 010, 011, 032, 041, 059, 064, 077, 078, 082, 083, 085, 086 — 17 rows, 86 total rows in the register, DEC-001 through DEC-086, no gaps or duplicates). The Technical-cell lag remains unreconciled — still not part of this pass's scope.

**Further correction (this pass, recording DEC-087):** the table above now includes DEC-087 (Product), bumping Product from 17 to 18 — verified via the same full per-entry tally method as the corrections above (87 total rows, DEC-001 through DEC-087, no gaps or duplicates). The Technical-cell lag flagged in DEC-084's correction note remains unreconciled — still not part of this pass's scope.

**Further correction (this pass, recording DEC-088):** the table above now includes DEC-088 (Deployment), bumping Deployment from 3 to 4 — verified via the same full per-entry tally method as the corrections above (88 total rows, DEC-001 through DEC-088, no gaps or duplicates).

## Maintenance Rules

- New decisions receive a permanent `DEC-` ID.
- Superseded decisions remain in the log.
- Affected documents must be listed.
- Implementation changes after IR-009 require change control.
- Not every documentation edit is a decision: pure reference-integrity fixes (correcting a stale version citation with no substantive content change) are recorded in the affected document's own Version History as a "housekeeping pass" and do not require a new `DEC-` entry.

## Validation Checklist

| Check | Status |
| --- | --- |
| Decision IDs defined | Complete |
| Categories defined | Complete |
| Active decisions recorded | Complete |
| Deprecated terms recorded | Complete |
| Traceability examples included | Complete |
| Maintenance rules defined | Complete |

## Version History

| Version | Status | Summary |
| --- | --- | --- |
| v1.1 | Frozen | Architecture Freeze decision log. |
| v1.2 | Frozen | Implementation Freeze alignment and consolidated active decisions. |
| v1.3 | Frozen | Recorded Lean Access Architecture (DEC-031), payment rail field (DEC-032), and week-3 stress-test checkpoint (DEC-033) under change control. Corrected title/Document Control version to match filename (was misstated as v1.2) and recorded documentation normalization (DEC-034), Lean Access propagation to 07/12/13 (DEC-035), database build-grade schema (DEC-036), shared-payment permission model (DEC-037), premium entitlement source of truth (DEC-038), reminder completeness and timezone handling (DEC-039), and measurable success metrics/test thresholds (DEC-040). |
| v1.4 | Frozen | Recorded the Retention Policy (DEC-041): active-user definition, Day 30 retention metric, monthly digest and lapsed-reengagement reminders. |
| v1.5 | Frozen | Recorded the brand kit finalization (DEC-042): color palette, typography, and logo for 05_Design_System, and the resulting cross-reference version bump across 00, 01, 02, 03, 04, 06, 07, 13, 14, 15, and 16. Updated dependency reference to 00_Project_Governance_v1.3. |
| v1.6 | Frozen | Recorded DEC-043: spacing/grid scale, card visual standard, and icon system for 05_Design_System, plus the decision to reject structural card imperfection in favor of content-driven variety. Bumped UX statistic 6 to 7. Updated dependency reference to 00_Project_Governance_v1.4. |
| v1.7 | Frozen | Recorded DEC-044: the full micro-interaction system (timing scale, button/card/toggle states, loading state, toast/dialog motion, no-motion rule for informational elements, reduced-motion fallback), including the correction to DEC-043's initial button and card hover values found to be insufficiently visible in prototyping. Bumped UX statistic 7 to 8. Updated dependency reference to 00_Project_Governance_v1.5. |
| v1.8 | Frozen | Recorded DEC-045: AI copy tone guidance for 02_Experience_Strategy's AI Experience Standard (sentence variety, state-once numbers, contractions permitted, no manufactured urgency, avoid corporate filler), completing the three-part "human touch" set alongside DEC-043 and DEC-044. Bumped UX statistic 8 to 9. Updated dependency reference to 00_Project_Governance_v1.6. |
| v1.9 | Frozen | Housekeeping pass, not tied to a new DEC: added a Maintenance Rules note distinguishing reference-integrity fixes from decisions, and updated the dependency reference to 00_Project_Governance_v1.8 following a citation-integrity check that found stale governance/cross-references in 01, 03, 04, and 14. |
| v1.10 | Frozen | Housekeeping pass, not tied to a new DEC: amended DEC-030's decision text to clarify it pins document 11's identity and origin (the user's existing document, source of truth for API contracts) rather than a frozen version number — 11 is now versioned through the same cascade as every other document, per the user's explicit direction, and was bumped to v1.1 to fix its own stale internal references. Added 11 to DEC-030's Affected Documents. Updated dependency reference to 00_Project_Governance_v1.8. |
| v1.11 | Frozen | Housekeeping pass, not tied to a new DEC: updated the dependency reference to 00_Project_Governance_v1.9, as part of the cascade closing 11's Depends On completeness (added 01, 03) and Supersedes-field fix, and 09's Architecture Integrity Review correction on the intentional 11/12 mutual dependency. |
| v1.12 | Frozen | Recorded DEC-046: `logo_url` column addition to `subscription_catalog`, the Music and AI Tools category additions, the two recategorizations, the JioHotstar/Spotify Premium renames, and the catalog expansion from 10 to 31 rows. Bumped Database statistic from 2 to 3. Updated the dependency reference to 00_Project_Governance_v1.10, as part of the cascade updating citations of 10_Database_Architecture's version bump to v1.5. |
| v1.13 | Frozen | Recorded DEC-047: the `post_renewal_checkin` reminder's 3-day-after-renewal, 9am-local offset, closing the ambiguity DEC-039 left open ("has just passed"), plus the finalized copy for all 8 `notification_templates` rows. Bumped Architecture statistic from 6 to 7. |
| v1.14 | Frozen | Housekeeping pass, not tied to a new DEC: updated the Depends On field to 00_Project_Governance_v1.11, closing the citation gap opened by this document's own v1.13 bump for DEC-047, batched with the notification-template SQL patch (file 23) and the doc 10 v1.6 citation cascade. |
| v1.15 | Frozen | Recorded DEC-048: business domain (`subsense.co.in`) and support email (`support@subsense.co.in`) registered, reversing the earlier no-domain assumption and unlocking real Supabase custom SMTP (doc 24 templates) and Resend domain verification (doc 25 templates). Bumped Deployment statistic from 2 to 3. No governing-document (00-16) citation impact — confirmed none reference a specific domain or sender assumption. |
| v1.16 | Frozen | Recorded DEC-049: adopted "Ledger Dark" as the finalized visual direction, superseding DEC-042's Monochrome Precision palette and two-family type system (Ledger Dark colors, single IBM Plex Sans family, 8px card radius). Bumped UX statistic from 9 to 10. No Depends On change (this document cites 00_Project_Governance, not 05, directly) — the cascade bumped 05_Design_System to v1.12 and 06_Component_Library to v1.11 (C-010's hardcoded "12px radius" corrected to "8px", "Accent" token references renamed to "Primary") in those documents directly. |
| v1.17 | Frozen | Recorded DEC-050: extended Ledger Dark to light-mode/email surfaces, closing the question DEC-049 left open. Bumped UX statistic from 10 to 11. No Depends On change — 05_Design_System bumped to v1.13 (Light-mode counterparts table), and files 24/25 (email templates, not governing documents) updated directly to match. |
| v1.18 | Frozen | Recorded DEC-051: extended DEC-050's Ledger Dark light-mode consistency to doc 18 (One-Page PRD), per explicit user confirmation. Bumped UX statistic from 11 to 12. No Depends On change — 18 (published summary, not a governing document) updated directly and bumped to v1.3. A follow-up pre-PRD audit caught that the v1.16 entry's "No Depends On change" claim was itself wrong: 00_Project_Governance moved to v1.12 (DEC-049) and then v1.13 (same-day oversight fix) during this same cascade, but this document's own Depends On field was never updated to track it and was still citing v1.11. Now corrected to 00_Project_Governance_v1.13. Not bumping the version again for this. |
| v1.19 | Frozen | Recorded DEC-052: introduced doc 26 (Expanded PRD) as a new tracked governing document — same Ledger Dark header/hero content and visual system as doc 18, but each section carries materially more depth sourced from docs 00, 01, 02, 07, 08, 09, 10, and 16, plus a new Key Decisions section (20 curated entries from this register). Bumped Documentation statistic from 1 to 2. No Depends On change — 26 is a new document, not a version bump of an existing one. |
| v1.20 | Frozen | Recorded DEC-053: frontend implementation moved from Lovable to Cursor (with Claude Code assistance) — a full replacement, caught when the user reviewed doc 26's Tech Stack section against actual practice. Dropped the standalone "Google API" stack card (Google Sign-In/DEC-011 unaffected, still via Supabase Auth) and Postman (no longer planned). Added the registered domain (`subsense.co.in`, DEC-048) as its own stack entry. No Depends On change — this decision touches body content (stack tables, cards, and rationale text) in 01, 04, 05, 07, 11, 15, 16, 18, 19, and 26, not this document's own dependency chain. |
| v1.21 | Frozen | Housekeeping pass, not tied to a new DEC: updated the Depends On field to 00_Project_Governance_v1.14, since 00 moved again within the same DEC-053 cascade this document's own v1.20 bump was part of. |
| v1.22 | Frozen | Housekeeping pass, not tied to a new DEC: updated the Depends On field to 00_Project_Governance_v1.15, its now-settled final version in this cascade. |
| v1.23 | Frozen | Housekeeping pass, not tied to a new DEC: updated the Depends On field to 00_Project_Governance_v1.16, since 00 moved again after 09 and 10 bumped a further time within the same DEC-053 cascade. Also corrected DEC-053's own Affected Documents field, which understated the cascade's real scope — it originally listed only 01, 04, 05, 07, 08, 11, 15, 16, 18, 19, 26 (the documents with substantive Lovable/Google-API/Postman/Domain content changes), but the citation ripple this decision triggered ultimately touched every document 00 through 16. Corrected the field to list 00-16 plus 18, 19, 26, as an accurate factual record of what was actually touched — not a new decision, so no new DEC-0XX entry. |
| v1.24 | Frozen | Recorded DEC-054: froze the Renewal Urgency Indicator's day-thresholds (Critical <= 2 days, Upcoming 3-7 days, Normal > 7 days), reusing the already-frozen two-day/seven-day reminder windows rather than a new number, needed because Phase 4 (Subscription Management) implementation asked directly and no frozen document defined it. Bumped UX statistic from 12 to 13. **Correction (same day):** the Depends On field did need a change after all — 00_Project_Governance bumped to v1.17 in this same cascade (closing its own stale citations to 05/08/16), so this field now cites 00_Project_Governance_v1.17. Not bumping the version again for this. |
| v1.25 | Frozen | Housekeeping pass, not tied to a new DEC: updated the Depends On field to 00_Project_Governance_v1.18, since 00 moved again closing the citation drift deliberately deferred since the DEC-054 pass (10, 11, 12, 13, 14, 15, 16, plus 00's own missed 01/02/03/04/07 rows caught in the same audit). Settled together with 00's own v1.18 bump in one pass rather than round-tripped, since the two documents cite each other. |
| v1.26 | Frozen | Recorded DEC-055: closed the logo wordmark font question DEC-049 left open (Space Grotesk vs. the app's actual IBM Plex Sans `font-heading` token) in favor of IBM Plex Sans, and formally recorded the logo's implementation (S-curve monogram, icon-only for in-app nav, full lockup for the pre-auth screen). Bumped UX statistic from 13 to 14. **Correction (same day):** the Depends On field updated to 00_Project_Governance_v1.20, since 00 moved twice more later in this same cleanup pass (closing its own 09-15 citation drift); settled together in one pass rather than round-tripped, since the two documents cite each other. |
| v1.27 | Frozen | Recorded DEC-056: the three pre-authentication screens (sign-in/sign-up, forgot password, reset password) carry a deliberate, scoped exception to the frozen Card visual standard and No Visual Noise/Five Second Rule principles — particle/sparkle background, 28px-radius glass-morphism card, kinetic/typewriter text motion (new tagline: "Track smarter. Renew wiser."), and a custom pill-shaped hover button as the primary CTA. Scoped to those three screens only; a product-wide visual direction change remains a separate, undecided question. Bumped UX statistic from 14 to 15. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.28 | Frozen | Recorded DEC-057: adopted a new user-supplied brand kit as SubSense's revised visual direction, superseding "Ledger Dark" (DEC-049/050) — amber/white accent system (#FFC800/#FFFFFF), a two-tier near-black background split (#050505 page, #0B0C0E card/elevated, chosen for text prominence), a four-family typography system (Syne, Cabinet Grotesk, Plus Jakarta Sans, Inter) replacing single-family IBM Plex Sans, and glass-morphism promoted from the DEC-056 pre-auth-only exception to the app-wide card standard (8px radius unchanged). Status colors, hairline borders, and light-mode/email tokens carried forward unchanged pending a future pass. Scope is the whole product including the DEC-056 screens, sequenced main-app-first. Four externally-sourced reference components (dot-grid background, gradient button, glowing-border card, collapsible sidebar) noted as candidate implementations requiring adaptation, tracked in `NEXT_SESSION_AGENDA.md`. Bumped UX statistic from 15 to 16. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.30 | Frozen | Recorded DEC-059: reverses part of DEC-043/046 — subscription cards no longer render a real per-service brand logo, only a fixed category-to-Lucide-icon mapping (inline, no URL), with the real subscription name still shown as plain text but only in the app's own typography, never a brand's own lettering. `logo_url`'s 30 real-logo values are nulled via a new SQL patch; the column itself is retained, unused. Bumped Product statistic from 10 to 11. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.29 | Frozen | Recorded DEC-058: resolved the three motion/background candidates left open after DEC-057. Dropped the dot-grid animated background candidate entirely in favor of the flat Page background token, no animation. Scoped the glowing-border card effect to render only on cards within the DEC-054 Critical/Upcoming renewal window (<=7 days), reusing that existing threshold rather than inventing a new one. Added Border Beam as a fifth reference component, scoped to run once on the outer app shell only, never per-card, as the product's single ambient/brand motion signature, distinct from the card glow's state-conveying role. Bumped UX statistic from 16 to 17. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.31 | Frozen | Recorded DEC-060: real image asset (`src/assets/subsense-logo-icon.png`) replaces the abstracted SVG monogram (DEC-055) as the icon everywhere — in-app and pre-authentication alike, reversing DEC-055's icon-only-in-app-vs-full-lockup-marketing split. Wordmark and tagline both moved to a two-tone color split ("Sub"/Primary + "Sense"/Foreground; tagline's two clauses split Foreground then Primary), tagline copy and wordmark font unchanged. `KineticText` gained an additive `hideCursorOnComplete` prop to prevent a double-cursor overlap when a two-tone element is built as two sequenced typewriter instances. Bumped UX statistic from 17 to 18. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.32 | Frozen | Recorded DEC-061: retired the single full-width Header bar (Global Navigation's reusable pattern list) as a distinct opaque top bar, replacing it with a background-less TopBar strip (hamburger + Logo/Home) above the Sidebar+Main row, plus an independently-floating TopBarActions cluster (Search/Add Subscription/Notifications/Profile menu) fixed to the top-right corner. Fixes Border Beam's structural invisibility across the top edge, which previously painted underneath Header's opaque background. Border Beam's own animation-parameter visibility and a possible glow-card extension to two Decision Workspace cards remain open, tracked in `NEXT_SESSION_AGENDA.md`, not part of this decision. Bumped UX statistic from 18 to 19. No Depends On change — 00_Project_Governance is still at v1.20. **Correction (same day, this pass):** the Decision Statistics table and Decision Categories list were both found stale during a follow-up check and reconciled against a full per-entry tally of the Active Decision Register — corrected counts (Governance 3, Product 10, UX 17, Architecture 6, Technical 4, Security 2, Testing 4, Deployment 3, Database 5, Frontend 3, Backend 2, Documentation 2, summing to the true 61 entries), added the previously-missing Frontend/Backend category labels, and removed the never-actually-used "Development" label. Not bumping the version again for this. **Further correction (found during the DEC-062/063/064 pass):** this row's own opening sentence ("Bumped UX statistic from 18 to 19") directly contradicted the correction immediately below it in the same row, which reconciled the true count to 17 — both cannot be right. The reconciled tally is ground truth (verified again during this same later pass, independently): UX was 17 as of DEC-061, not 19. The "bumped to 19" claim was itself stale shorthand, superseded by the reconciliation, and should be read as void. Not bumping the version again for this. |
| v1.33 | Frozen | Recorded DEC-062: extended the renewal-urgency glow-card effect (`GlowingEffect`, DEC-058) to a second, distinct meaning — page-section importance — applied unconditionally to `DecisionWorkspacePage`'s "Today's Financial Context" and "Recommended Reviews" sections, plus a props-only Border Beam retune (`size` 200 to 320, `duration` 30s to 10s, `borderWidth` 1 to 2) on its existing shell mount point, staying within DEC-058's original scope rather than relocating it. Bumped UX statistic from 17 to 18. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.34 | Frozen | Recorded DEC-063: folded the standalone `GradientButton` candidate (DEC-057, never adopted) into the shared `buttonVariants` system as `variant="gradient"`, scoped to the app's 5 primary "Add/Save Subscription" CTAs only, with a `prefers-reduced-motion` fallback (the first in the codebase) and an inline-style background-delivery fix for a real rotation-rendering bug found during build. Bumped UX statistic from 18 to 19. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.35 | Frozen | Recorded DEC-064: added card-level Paid/Paused/Resume quick-actions to `SubscriptionCard`, a new write path alongside the existing View -> Edit pattern. Paused now genuinely excludes a subscription from urgency calculations, Upcoming Renewals, and Recommended Reviews (closing a previously-missing filter on Upcoming Renewals); Paid recomputes `next_renewal_date` only via a new `computeNextRenewalDate` helper, deliberately leaving the dormant `renewal_confirmed` lifecycle status untouched pending its own future decision. Bumped Product statistic from 10 to 11. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.36 | Frozen | Recorded DEC-065: adopted the "Cyber Lime" brand kit as SubSense's revised visual direction, superseding the amber kit (DEC-057) — full token rewrite (lime Primary Accent replacing amber, reintroduced two-tier surfaces, solid-hex Border/Border Strong, new Secondary Accent/Disabled roles), a 3-step Secondary button hover/active progression, a 45-degree lime-to-blue wordmark gradient scoped to the three actual pre-auth wordmark instances, `AuthPage.tsx`'s kinetic wordmark/tagline dropped in favor of static text (`KineticText.tsx` deleted, zero other consumers), and the new tagline "Think wiser. Choose smarter." An explicit 60-30-10 usage-discipline note was added for status colors (unchanged hex, confined to small indicators, confirmed via investigation no violation exists today). Bumped UX statistic from 19 to 20. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.37 | Frozen | Recorded DEC-066: retired DEC-056's pre-authentication visual exception now that DEC-065 shipped — particle background and its `@tsparticles/*` dependencies removed outright, the glass-morphism card collapsed to the standard opaque card treatment (closing a real doc-vs-shipped-code gap found during investigation: the actual "standard" card is opaque on 9 of 10 page-level instances, not the glass treatment this document previously described as universal), and the custom pill CTA replaced by the standard `Button` component (`AuthPage`'s CTA specifically takes `variant="gradient"`, a deliberate front-door-vs-utility-flow distinction from `ForgotPasswordPage`/`ResetPasswordPage`, which stay on `variant="default"`). Two follow-up polish fixes included: the Google button's undocumented `rounded-full` override removed, and the hero/card layout gap tightened (`justify-center` replacing `justify-between`, reusing the existing `gap-16` value). Bumped UX statistic from 20 to 21. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.38 | Frozen | Recorded DEC-067: fixed two Decision Workspace display bugs found during live review — `upcomingRenewals` gained a second filter reusing `computeRenewalUrgency` (dropping the >7-day "normal" tier, per DEC-054's Upcoming ceiling and DEC-039's `seven_day` window), previously absent entirely; "Recommended Reviews" swapped from a populated, duplicate-of-Upcoming-Renewals list to a static honest placeholder matching its three genuine not-yet-built siblings (AI Insights, Shared Payment Activity, Potential Savings), partially reversing DEC-062's unconditional `GlowingEffect` on that section. Investigation confirmed `review_due` is a dormant lifecycle-status enum value (same situation as DEC-064's `renewal_confirmed`) — nothing in the codebase writes it, so no OR-clause was needed to preserve visibility for it. Bumped UX statistic from 21 to 22. No Depends On change — 00_Project_Governance is still at v1.20. Flagged, not resolved, in DEC-067's own text: doc 04's Decision Workspace Blueprint still names a single "AI Insight" Primary-content line with no "Recommended Reviews" entry — a real, pre-existing doc-vs-code gap this pass does not fix, tracked in `NEXT_SESSION_AGENDA.md` as an open question. |
| v1.39 | Frozen | Housekeeping pass, not tied to a new DEC: closed the open question DEC-067 itself had flagged — the user confirmed "AI Insights" and "Recommended Reviews" are one concept split across two UI slots for now, not two distinct Primary content items. Amended DEC-067's own entry above with a "Resolved" note and added 04 to its Affected Documents field. Doc 04 updated with a new Implementation notes bullet under Decision Workspace Blueprint stating the two-slot split explicitly (bumped to v1.18) — its Primary content list itself is unchanged, since this doesn't warrant naming "Recommended Reviews" as its own item. No Decision Statistics change — still 67 total entries, no new DEC row added. |
| v1.40 | Frozen | Recorded DEC-068: formalized Phase 6's reminder-scheduling platform (Supabase Cron, over raw `pg_cron` SQL or an external scheduler) and job shape (an hourly `send-reminder-email` delivery job querying `status = 'pending' AND scheduled_for <= now()`, plus a new daily `generate-scheduled-reminders` job covering the three day-granularity types — `post_renewal_checkin`, `monthly_digest`, `lapsed_reengagement` — that had no concrete generation mechanism named in doc 11 before this). No dedicated retry/fallback or cron-failure alerting system is built at MVP scale: confirm-before-marking-sent and a `<=`-based catch-up query, both already achievable within the existing `pending`/`sent`/`skipped_archived`/`failed` status enum, are relied on instead; active monitoring is deferred to production scale, tracked in `NEXT_SESSION_AGENDA.md`. Bumped Architecture statistic from 6 to 7. Doc 10 bumped to v1.12 (completed generation-mechanism table and due-today evaluation note), doc 11 bumped to v1.10 (completed Section 4's generation description, updated 5.2, added new Section 5.7 for `generate-scheduled-reminders`), and doc 12 bumped to v1.12 (completed the Reminder Service row) — all three cited each other's new versions in the same pass. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.41 | Frozen | Recorded DEC-069: ships `28_SubSense_Scheduled_Reminders_Patch_v1.0.sql`, the DB-side migration implementing DEC-068's `generate-scheduled-reminders` mechanism — loosens both `reminders_target_present` and `reminders_shared_payment_target` CHECK constraints (the latter never previously documented in doc 10 at all — a real pre-existing gap, not introduced by this pass) to allow `monthly_digest`/`lapsed_reengagement` rows with `subscription_id` and `payment_request_id` both null, and adds a new `audit_action` enum value, `reminder_generation_failed`, distinct from `reminder_executed` (an actual send). Bumped Database statistic from 5 to 6. Doc 10 bumped to v1.13 (both constraints now documented, nullability text corrected, `audit_action` enum values listed, Generation mechanism table cites file 28). No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.42 | Frozen | Recorded DEC-070: ships `29_SubSense_Reminder_Lifecycle_Patch_v1.0.sql`, fixing a real bug DEC-064 had explicitly flagged and deferred — `generate_default_reminders()` never cancelled stale pending reminders when `next_renewal_date` changed or a subscription was paused, confirmed live today when DEC-069's now-working delivery pipeline sent several reminders whose content contradicted the subscription's current state (a renewed-forward subscription still got "renews today"; a paused one still got all three of its default reminders). Also confirmed the bug is worse than cosmetic: doc 10's existing partial unique index on pending reminders means a stale pending row silently blocks the next cycle's reminder of the same type from ever being inserted. Adds a new `reminder_status` value (`skipped_superseded`, one generic value for both causes) and a new trigger (`subscriptions_cancel_stale_reminders`, ordered to fire before the existing generate trigger) that cancels stale pending reminders on a renewal-date change or entering `'paused'`, and regenerates the default set on resuming from `'paused'`. Bumped Database statistic from 6 to 7. Doc 10 bumped to v1.14 (new enum value, new trigger/helper function documented, Generation mechanism table updated). Run against the live Supabase project and verified. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.43 | Frozen | Recorded DEC-071: ships `30_SubSense_Post_Renewal_Checkin_Paused_Fix_v1.0.sql`, closing a narrow residual gap found by walking DEC-070's fix through every subscription-state transition — `generate_scheduled_reminders()`'s `post_renewal_checkin` query never checked `lifecycle_status`, so a daily cron run could still generate a fresh check-in for an already-paused subscription if its frozen renewal date happened to line up. One-clause fix: `and s.lifecycle_status <> 'paused'` added to that query's WHERE clause, everything else in the function unchanged from file 28. Bumped Database statistic from 7 to 8. Doc 10 bumped to v1.15 (post_renewal_checkin generation description updated with the exclusion). No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.44 | Frozen | Recorded DEC-072: corrected a live drift where Supabase's Confirm signup/Magic link/Reset password auth templates had been pasted as plain unstyled HTML instead of doc 24's branded "Ledger Dark" light-mode blocks — found by comparing doc 24's source against screenshots of what's actually live, fixed by re-pasting doc 24's existing HTML verbatim (no content or design change). Also confirmed Supabase auto-generates a 4th auth template ("Confirm your new email address") that doc 24 never specified; decided to leave it as Supabase's plain default, out of MVP scope, and recorded the gap in doc 24 itself rather than silently absorbing it. Bumped UX statistic from 22 to 23. Doc 24 bumped to v1.2 (new Version History note recording the change-email gap). No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.45 | Frozen | Recorded DEC-073: resolved the light-mode/email accent gap DEC-057 opened and DEC-065 carried forward unchanged a second time — prompted by the user noticing DEC-072's just-repasted templates still rendered in the original Ledger Dark blue against the app's current Cyber Lime look. Retired 05_Design_System's single "Accent (light)" token in favor of two: Accent Fill (light) (#A3E635, raw Primary hex, button backgrounds only) and a new Accent Text (light) (#4D7C0F, a darkened same-hue derivative for wordmark/text use, since the raw hex fails WCAG AA as text on white at ~1.9:1). Button text on Accent Fill (light) is Text Primary (light) (#0F172A), not white, mirroring the in-app Primary button's dark-text-on-lime rule. Bumped UX statistic from 23 to 24. Doc 05 bumped to v1.31 (Light-mode counterparts table rewritten with rationale). Doc 24 bumped to v1.3 (3 templates recolored, re-pasted). Extended same-day to doc 25 (v1.2, 7 reminder templates recolored) and the live `_shared/email-template.ts` shell (committed `39358cd`, pushed, verified clean). No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.46 | Frozen | Recorded DEC-074: designed a logo-icon-plus-text header treatment (real logo icon, DEC-060, alongside the existing text wordmark, nested-table layout for Outlook compatibility, fixed public asset URL) for every light-mode/email template. **Same-day follow-up:** the user decided not to implement it during the capstone phase, after reviewing the full scope (a `public/` asset copy plus edits to docs 24/25 and the live `_shared/email-template.ts` code) — deliberate MVP scope discipline. Doc 24 (v1.4) and doc 25 (v1.3) record the exact markup as a reference note only; their actual template bodies stay text-only, matching what's genuinely live. Bumped UX statistic from 24 to 25. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.47 | Frozen | Recorded DEC-075: closed the last open item DEC-072 flagged (whether to draft a branded template for Supabase's "Password changed" Security notification, a dashboard section distinct from the Templates tab). Decision: yes — added TEMPLATE 4 to doc 24 (v1.5), styled identically to TEMPLATES 1-3, but with a CTA linking to Subsense-web's own `/auth/forgot-password` route via `{{ .SiteURL }}` instead of `{{ .ConfirmationURL }}`, since this notification type is informational and doesn't populate that variable. Subject line matches Supabase's own default, "Your password was changed." Bumped UX statistic from 25 to 26. Docs-only this pass — the user still needs to paste the block into Security notifications (not Templates) and enable the notification toggle live. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.48 | Frozen | Recorded DEC-076: live-testing DEC-075's new CTA link surfaced a real, previously-undetected production bug — no `vercel.json` exists in Subsense-web, so Vercel 404s on direct navigation to any nested client-routed path (no SPA catch-all rewrite configured). Confirmed this isn't cosmetic: the real `resetPasswordForEmail()` flow redirects to the identical broken path pattern (`/auth/reset-password`), so real users clicking the actual password-reset email link were very likely hitting this 404 too, not just the new template's CTA. Fix: `vercel.json` with a catch-all rewrite to `index.html`, handed to the user as a Claude Code prompt — not yet committed/deployed/verified as of this entry. Doc 15 bumped to v1.11 (new SPA Routing note recording the requirement, previously undocumented). Bumped Technical statistic from 4 to 5. Cascaded through 00, 07, 16. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.49 | Frozen | Recorded DEC-077: confirmed via full-codebase grep (`signInWithOtp`, zero hits) that Magic Link sign-in was never actually implemented in Subsense-web, despite doc 24's TEMPLATE 2 styling Supabase's default Magic Link template — the user correctly identified this while testing and it was confirmed, not assumed. Decision: defer scoping real Magic Link sign-in until/unless the product continues past capstone, same deliberate MVP discipline as DEC-074. Doc 24's TEMPLATE 2 left as-is but flagged in its own header comment as describing an unused-but-ready template, not an oversight. Doc 24 bumped to v1.6 (flag note only, no template content change). Bumped Product statistic from 11 to 12. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.50 | Frozen | Recorded DEC-078: a careful, evidence-checked Phase 1-6 completion audit (files/migrations/code checked directly, not doc 19's status bullet alone) found doc 16 Phase 6's "Implement Send Reminder Now in Developer/Test Utilities" deliverable was never actually built as an in-app page — confirmed via a full-repo grep and route-list review, zero matches. The underlying single-send capability itself works and is live-verified (`send-reminder-email`'s `{ reminder_id }` mode, invoked directly multiple times this session) — only the UI surface is missing. Decision: defer building it until Phase 11 (Developer/Test Utilities), where doc 16 already groups it with Test AI Response/Test Email Payload/Test Razorpay Payment/View integration status — building it now would mean a throwaway single-utility page or a premature partial Phase 11 built before Phase 7/10 exist to populate two of its five utilities. Doc 19's Phase 6 status bullet corrected to name this deferred deliverable explicitly rather than stand as an unqualified "functionally complete." No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.51 | Frozen | Recorded DEC-079: formalized Phase 7's (AI Decision Support) runtime architecture ahead of any Edge Function code being written, mirroring how DEC-068 settled Phase 6's platform/job-shape questions first. Trigger model: lazy-generate on read (Path A reads the latest `ai_recommendations` row; `ai-generate-insight`, Path B, only fires when no row exists, the row is over 24 hours old, or `next_renewal_date`/`lifecycle_status` changed since `generated_at`, plus a manual Regenerate action) — no cron job, since insight reads follow unpredictable user navigation, not a fixed daily tick. Workspace batch scope: Decision Workspace's AI Insight slot covers at most the 3 subscriptions in Critical/Upcoming urgency (`computeRenewalUrgency`, DEC-054), not every active subscription. Model choice: `gpt-4o-mini`. Duplicate subscription detection — named in doc 18's PRD copy but never part of doc 16's actual Phase 7 acceptance criteria — confirmed as a real doc 18-vs-doc 16 scope gap and deferred to Phase 9 (Insights), whose "show savings opportunities where available" goal is the natural home; doc 16 bumped to record this explicitly in Phase 9 so it's picked up automatically rather than silently dropped. Bumped Architecture statistic from 7 to 8. Doc 16 bumped (Phase 7 deferral note, Phase 9 goal added); doc 04 bumped (Decision Workspace Implementation notes: batch-scope rule stated explicitly). Cascaded through every document citing 04, 08, or 16. No Depends On change — 00_Project_Governance is still at v1.20. |
| v1.52 | Frozen | **Same-day extension of DEC-079** (no new DEC number — this fills in implementation-level detail, not a new architectural decision): a Claude Code planning pass against the locked DEC-079 architecture (plan-only, nothing built) surfaced two real contract gaps and flagged one real complication, all confirmed with the user before the plan was approved. (1) Batch response shape: doc 11's flat `ai-generate-insight` response can't hold a 3-subscription workspace batch — resolved via an `insights[]` array wrapper for `{scope:"workspace"}` only, single-subscription shape unchanged; doc 11 bumped to v1.11. (2) AI Insights/Recommended Reviews merge: DEC-067's unresolved "one concept, two UI slots" framing resolved to one merged "AI Insights" section (doc 06's C-003 has no second component to preserve); doc 04 bumped to v1.20. (3) Staleness-rule proxy: no snapshot of `next_renewal_date`/`lifecycle_status` exists to compare against, so `subscriptions.updated_at > generated_at` is used as a conservative, slightly-more-eager-never-less proxy — no schema change. Plan reviewed and approved this session; build has not started, pending the user's explicit go-ahead. Affected Documents for DEC-079 updated from "04, 16" to "04, 11, 16." Cascaded through every document citing 04, 08, or 11. No Depends On change — 00_Project_Governance is still at v1.20. **Further correction (same day, build completed and live-verified):** Claude Code built and deployed Phase 7 (11 files: `ai-generate-insight` + 4 `_shared` helpers, 4 frontend files, 2 pages wired). Two implementation-time issues found and fixed before calling it done: an undocumented CORS gap (this is the first Edge Function ever called directly from the browser; `_shared/http.ts` had no preflight/CORS handling, fixed with a wildcard-origin policy since auth is Bearer-token, not cookie-based) and an AI Copy Tone violation (the model was restating the subscription's cost, already shown twice elsewhere on the card — fixed by removing cost/currency from the model's context entirely, not just instructing it to omit them). Live-verified: real AI Decision Card renders correctly, archived-subscription suppression confirmed (no OpenAI call, Regenerate/Try again both locked out), and Paused-subscription suppression added and confirmed (extends DEC-064's existing paused-deprioritization pattern to this surface — see the DEC-079 register entry's own further-resolved note above). Doc 04 updated (Subscription Details Implementation notes now state the Paused/Archived suppression explicitly). Not bumping the version again for this. **Further same-day extension:** the user identified a residual risk in the staleness proxy — resuming a Paused subscription always bumps `updated_at`, so repeated pause/resume cycling followed by a page visit could retrigger real OpenAI calls with no cap. Fixed with a 5-minute `MIN_AUTO_REGENERATE_INTERVAL_MS` floor in `shouldRegenerateInsight()` (`ai-insight-utils.ts`), scoped to the silent auto-trigger path only (manual Regenerate stays immediate, confirmed structurally exempt). Live-verified: rapid pause/resume no longer triggers a call; manual Regenerate still does. One false-positive repro during testing was traced via temporary instrumentation (committed, reproduced clean, then reverted) and ruled out as a stale pre-rebuild bundle, not a logic bug. Not bumping the version again for this either — implementation detail within the already-locked architecture. |
| v1.53 | Frozen | Recorded **DEC-080**: formalized four Phase 8 (Shared Subscriptions) architecture forks — payment-request generation (new DB trigger extending DEC-070's pattern, fires on `next_renewal_date` change, not lazy-generate or a scheduled job), the "Share this subscription" entry point (new action on Subscription Details alongside Paid/Paused/Archive, DEC-064), equal-split computation (auto-computes and rebalances on membership change, per-cycle `payment_requests` snapshots stay untouched), and member-removal cleanup (no cascade-cancel of open requests, contrasting deliberately with the existing archive-cascade rule) — resolved via direct interview (recommendation + trade-off presented for each fork, user chose all four recommended options), mirroring the DEC-068/DEC-079 precedent of settling architecture before any build prompt. Component-level UI specs for the Shared Subscriptions page itself remain open, flagged for the next pass rather than resolved here. Bumped Architecture statistic from 8 to 9 (also caught and retroactively logged a documentation gap: DEC-079's own Architecture bump, 7 to 8, was never narrated with its own correction note, though the table itself was already correct). Doc 10 bumped (new trigger, rebalance rule, and explicit non-cascade note documented); doc 04 bumped (Share entry-point Implementation notes bullet added). Cascaded through every document citing 04, 08, or 10. No Depends On change — 00_Project_Governance is still at v1.20. **Further correction (same day, this row missed narrating the component-spec closure already recorded in DEC-080's own register entry above):** three new doc 06 components (C-019 Shared Member Row, C-020 Add/Edit Member Form, C-021 Payment Request Item) closed the "component-level UI specs remain open" line above the same session it was written — this row is corrected to note that gap did not, in fact, stay open past this pass. Doc 06 bumped to v1.24; Affected Documents for DEC-080 updated from "04, 08, 10" to "04, 06, 08, 10." Not bumping this document's version again for this. **Further correction (same day, DEC-080 same-day extension — Phase 8 technical-planning gaps):** a Claude Code planning pass (plan-only, twice reviewed and revised, not yet built) surfaced two real implementation gaps and two accepted, explicitly-documented tradeoffs — a second `payment_requests`-generation trigger firing on `shared_members` insert (closing a gap where a brand-new share or mid-cycle new member generated zero requests until the next renewal), `reminders.user_id`/`notifications.user_id` loosened to nullable for unlinked-member `shared_payment` rows (mirroring DEC-069's target-presence-loosening pattern, with recipient-email resolution falling back to `shared_members.email`), the equal-split rounding remainder accepted as a documented MVP simplification, and a known RLS gap on null-`user_id` reminders/notifications rows accepted since no locked component spec surfaces that data in the UI. See DEC-080's own register entry above for full detail. Doc 10 bumped to v1.17. Not bumping this document's version again for this either. |
| v1.54 | Frozen | **DEC-080 further extension, this time from live-testing the built feature** (no new DEC number — same architecture, real bugs found and fixed against it): (1) the equal-split divisor never counted the owner as a sharer, corrected to `active_count + 1`; a still-`pending` `payment_requests` row now stays synced to the current split instead of freezing at creation; icon-only action buttons given accessible tooltips — all via `32_SubSense_Equal_Split_Fixes_v1.0.sql` plus a frontend-only tooltip change. (2) `shared_members.user_id` was never populated by the add-member flow, so the "linked member" RLS grants could never fire for anyone — fixed with a new email-matching `before insert` trigger, `33_SubSense_Link_Existing_User_On_Member_Add_v1.0.sql`; retroactive linking on later signup remains an accepted, deferred gap. (3) A PostgREST embed-swallow bug silently hid an otherwise-visible `shared_subscriptions` row from a linked member — fixed with a new, additive RLS policy (`34_SubSense_Shared_Member_Subscription_Visibility_v1.0.sql`, an accepted full-row grant) plus an embed-hint hardening in the frontend query. (4) That new grant's wider blast radius — a linked member could reach the owner-only subscription details page and see the share under their own "My Subscriptions"/Decision Workspace — closed with an owner-redirect guard and explicit owner filters on both affected list queries. (5) A related bug found in the same pass: a write silently blocked by RLS was being reported as a success toast; audited and fixed across all 8 vulnerable mutation call sites codebase-wide, not just the one caught live. One gap found but not yet fixed, carried to `NEXT_SESSION_AGENDA.md`: a removed member's own still-pending request is designed to stay visible to them at the RLS level, but the frontend's query structure hides it anyway once they lose visibility into the parent share. See DEC-080's own register entry above for full detail. Doc 10 bumped to v1.18. Not bumping this document's version again for this either. |
| v1.56 | Frozen | **Recorded DEC-083 (Product):** Phase 9+10 (Insights, Premium Gating, Razorpay Demo Purchase) recorded as implemented, closing DEC-082's "decision recorded only" status — Profile page's Plan Comparison Card/Upgrade flow, Decision Workspace's real `is_premium` gating, and the Phase 9 Insights page all built and deployed this session, per three plan-review rounds that caught a receipt-length overflow, the existing signature-scrub trigger's interaction with the verification write, and a replayable premium-extension bug (fixed by keying computed expiry off the transaction's own `verified_at`, not `now()`). Two new error codes recorded for doc 11 (PAY_004, PAY_005). Explicitly **not** treated as fully closed: three issues found live-testing (an AI-summary currency-drop bug, a genuine full-page-reload-on-navigation bug not yet root-caused, and the absence of any session-idle-timeout policy) remain open, tracked in `NEXT_SESSION_AGENDA.md`, alongside a proposed-but-not-yet-built rename of the Cost Comparison Card's overpromising "Lower-Cost Alternatives" label. Corrected a retroactive Decision Statistics gap found while adding this entry (DEC-082's Product bump to 14 was narrated but never actually applied to the table). Doc 04 (Profile/Insights blueprints), doc 06 (already bumped to v1.25 earlier this session for the 8 new components), doc 10 (`is_premium` reader resolved), doc 11 (Sections 5.5/5.6 marked built, new insights-generate-summary section, PAY_004/PAY_005), and doc 16 (Phase 9/10 status) all updated and cascaded. **Correction (same day, full grep audit):** the Depends On field, found stuck at 00_Project_Governance_v1.20, corrected to v1.21, since 00 moved again later in this same cleanup pass. Not bumping the version again for this. |
| v1.57 | Frozen | **Recorded DEC-084 (Frontend):** corrects DEC-083's "not yet root-caused" status for the full-page-reload-on-navigation bug with a confirmed root cause, found by reading `@supabase/auth-js`'s shipped source directly rather than assumed. Live reproduction this session (narrowed precisely by the user's own testing) established the reload only ever fires after a tab/app switch and return, never from in-app navigation — `App.tsx`/`Sidebar.tsx` were correctly cleared by DEC-083's own check. The actual cause: `GoTrueClient`'s `visibilitychange` listener re-notifies every `onAuthStateChange` subscriber (`SIGNED_IN` or `TOKEN_REFRESHED`) on essentially every tab refocus, and `AuthContext.tsx` ran its full state-reset (`applySession()`, including `setLoading(true)`) for every such event regardless of whether the identity actually changed — `ProtectedRoute.tsx`'s full-screen spinner then blanked the entire app on every refocus. Fix: `AuthContext.tsx` now tracks the last-applied identity via a ref and only runs the full reset on a genuine identity change, otherwise just refreshing the session token quietly. Scope note recorded because it reversed in-progress work: a page-level stale-while-revalidate caching hook, drafted and wired into 7 files before live reproduction narrowed the actual trigger, was fully reverted once recognized as solving a different problem than the one observed (`ProtectedRoute`'s spinner blanks everything regardless of per-page caching) — not shipped alongside the real fix, to keep this change scoped. Bumped Frontend statistic from 3 to 4 (also flagged, not fixed, a pre-existing lag in the Technical cell found during this pass — see Decision Statistics). No doc 00-26 content edited this pass; docs 10, 11, 16, and 19 each still carry DEC-083's now-stale "not yet root-caused" status language, flagged here as pending a follow-up correction rather than fixed silently. No Depends On change — 00_Project_Governance is still at v1.21. |
| v1.58 | Frozen | **Recorded DEC-085 (Product):** closes the two remaining open items from DEC-083's live-testing pass (the currency-drop bug and the Cost Comparison Card's overpromising name), both built and deployed this session. **Currency-drop fix:** `insights-generate-summary/index.ts` now builds a templated lead sentence directly from the already-computed `spendSummary` (e.g. "You're spending ₹868.00 and $9.00 across 4 active subscriptions this month," single-currency portfolios getting the same pattern with one figure), and the OpenAI call is restricted to a short qualitative framing sentence only, appended after the templated lead. A code-level guard discards the model's framing text and substitutes a static fallback sentence if it matches `/\d/` or contains `₹`/`$` — a prompt instruction alone was rejected as insufficient given this is the exact bug being fixed; the guard makes recurrence structurally impossible, not just less likely. **Cost Comparison Card rename:** the response key (`lower_cost_alternatives` → `cost_comparisons`), type (`LowerCostAlternative` → `CostComparisonItem`), and function (`findLowerCostAlternatives` → `findCostComparisons`) all renamed together as one coupled commit (a breaking wire-contract change between the Edge Function and frontend). Card display name changed to "Worth a Second Look" (doc 06 C-029's own already-proposed direction), per-item copy reworded to name the specific percentage-above-average and invite review rather than imply a cheaper alternative exists, and the AI summary prompt's own language for this signal updated to match. Existing Review Subscription/Remind Me Later actions reused — no new CTA, since no data exists to back a "switch to X" suggestion. Both fixes committed and pushed same session; **live verification (2-currency ×5 regeneration, single-currency regression, deliberate temptation case, and visual confirmation of the new copy against both an above-average and a below-average subscription) is the first task next session, not yet completed as of this entry** — not claimed as fully closed. Bumped Product statistic from 15 to 16. Doc 06 (C-029 naming note) and doc 11 (§5.8 currency-guard mechanism, response-shape rename) both updated this pass. The session's fourth tracked item, idle-session timeout, remains untouched — still gated on producing a code-free plan and a separate go-ahead before any code, per DEC-083's own recording. No Depends On change — 00_Project_Governance is still at v1.21. **Further correction (same day, second-order cascade closure):** the Depends On field's own 00_Project_Governance citation, one hop stale after bumping 00 during this same session's cascade, corrected to v1.22. Not bumping the version again for this. |
| v1.59 | Frozen | **Closes out DEC-085's live-verification pass, both items confirmed live, plus a genuine deploy-gap bug found and fixed during that verification.** The Cost Comparison Card rename ("Worth a Second Look") and the currency-drop fix (2-currency portfolio, multiple reloads, both totals present every time, matching the Spend Summary card) are both confirmed live-working — full detail folded into DEC-085's own Rationale cell above rather than repeated here, matching this project's existing amend-in-place convention. **Deploy-gap bug, found during this verification, now resolved:** the Insights page crashed outright (`Cannot read properties of undefined (reading 'length')`, indefinite loading then a blank screen) because the live Edge Function was still serving the pre-DEC-085 `lower_cost_alternatives` response shape against an already-deployed frontend expecting `cost_comparisons` — not a code defect, but a failed "Deploy Supabase Edge Functions" GitHub Action run for the same push, caused by a transient GitHub-side runner-provisioning outage ("job was not acquired by Runner… Internal server error"), confirmed via the Actions log and not a defect in the workflow file, secrets, or the Supabase CLI. Fixed by manually re-running the same unchanged workflow (succeeded in 44s) — no code or config change needed, so this gets a Version History note rather than its own DEC entry (nothing was decided or changed; a transient CI failure was simply retried). No Depends On change. |
| v1.60 | Frozen | **Recorded DEC-086 (Product):** closes the fourth and final item DEC-083 tracked open — session-idle-timeout, built and live-verified this session. Locked decisions: per-tab idle tracking (accepted limitation, no cross-tab sync), a "Stay signed in" toast action, and demo-week thresholds of 30 seconds (warning) / 40 seconds (sign-out) — down from an initial 15/20-minute build, retuned twice same-day per the user's demo-time constraints; revisit to ~4/4.5 minutes if this continues past the capstone as a real product, since even 15/20 minutes was judged too lax for that later stage. Implementation: new `useIdleTimeout.ts` hook mounted from `ProtectedRoute.tsx`, timestamp-based idle measurement (not re-armed setTimeouts) polled every second, reusing the existing Sonner toast pattern and `AuthContext.tsx`'s existing `signOut()` — `AuthContext.tsx`'s own DEC-084 identity-change logic untouched. A `checkout-state.ts` module (deliberately outside AuthContext) tracks whether Razorpay's checkout overlay is open; the idle timer suspends (freezes measured idle duration, does not reset or pause) while it's open, since the checkout iframe emits zero activity events on the parent document and either resetting or ignoring this case would be wrong in one direction or the other. `UpgradeButton.tsx` clears the flag on all three exit paths (success, failure, and modal dismiss). A real toast-copy bug was caught and fixed during the demo-value retune: whole-minutes rounding produced nonsensical copy ("0 minutes") at sub-minute thresholds, fixed with a new `formatDuration()` helper. Manually live-verified: warning toast, "Stay signed in" reset, real sign-out, and checkout-suspension all confirmed working at the locked 30s/40s values. Build/lint clean; no test runner configured in this repo, flagged per CLAUDE.md's own instruction rather than silently skipped. Committed and pushed across two passes: `d1d6d48`/`d2d83ab` (original 15/20-minute build) and `44053bb` (demo-value retune + copy fix), `origin/main` confirmed matching local HEAD after each. Bumped Product statistic from 16 to 17 (also caught and fixed a retroactive gap: DEC-085's own Product bump, 15 to 16, was narrated but never actually applied to the table — see Decision Statistics). Affected Documents: 16, 19 only — no API contract, schema, or Path A/Path B architecture change, and no new reusable component (the warning toast reuses the existing Sonner pattern already wired project-wide), so no doc 06, 10, or 11 change is needed. No Depends On change — 00_Project_Governance is still at v1.22. |
| v1.61 | Frozen | **Recorded DEC-087 (Product):** closes out Phase 11 (Developer/Test Utilities) and Phase 12 (Testing/QA), planned, built, and tested together this session. Full detail in DEC-087's own register entry above rather than repeated here, matching this document's amend-in-place convention. Summary: all five Phase 11 utilities built behind a single `dev-utilities` Edge Function, restricted mid-session to a 3-account allowlist (client- and server-side) rather than doc 16's originally-planned any-authenticated-user access; two real bugs found and fixed in already-shipped code (`send-shared-payment-reminder`'s missing `updated_at` back-date, `SubscriptionCard`'s nested interactive controls, WCAG 4.1.2); Phase 12 deliberately rescoped from a full RTM cycle to a lightweight three-area pass (E2E, targeted RLS/premium spot-check, accessibility) per the user's own challenge; all nine live tests passed; four more real bugs found and fixed purely because live testing exercised the app directly — a false-negative in the Resend integration-status health check, Decision Workspace's "Shared Payment Activity" widget being an unbuilt static placeholder (now built and scoped to a 7-day window), the "Potential Savings" placeholder hidden from view (UI-only, still unbuilt), and a mobile-viewport accessibility bug on the global `TopBarActions` button (fixed once, cleared everywhere it mounts). Bumped Product statistic from 17 to 18. Doc 04 updated (Decision Workspace Implementation notes: Shared Payment Activity built-and-scoped, Potential Savings hidden-not-built). Doc 16 updated (Phase 11+12 status flipped from planned to built-and-tested, citing this entry). No doc 06, 10, or 11 change — no new DB schema, no new reusable component, and `dev-utilities`' own action set was never an externally documented API contract (a dev-only surface per doc 16's own restriction). **Further correction (same day, second-order cascade closure):** the Depends On field's 00_Project_Governance citation, one hop stale after 00 bumped again during this same closure pass, corrected to v1.23. Not bumping the version again for this. |
| v1.62 | Current | **Recorded DEC-088 (Deployment):** closes out Phase 13 (Deployment) by documenting the actual as-built deployment reality — production-only deployment via Vercel/GitHub Actions since Phase 6, no staging tier ever stood up, rollback handled in practice via `git revert` plus the same forward auto-deploy pipeline, and the formal verification checklist never run as a single documented gate (each phase's own live-testing covered the same ground informally instead). Full detail in DEC-088's own register entry above. Bumped Deployment statistic from 3 to 4. Doc 15 updated (new "As-Built Deployment Record" section, bumped to v1.13) and doc 16 updated (Phase 13 section rewritten from unstarted plan to built-and-closed record, bumped to v1.25), both citing this entry. |
